---
type: cdd-iteration
scope: wave-level (cross-cycle)
date: 2026-05-20
source: "paired-cycle wave on cph: cph#27 (R3 R-side aggregate condition-response analysis) + cph#28 (L-cycle recovery — contralateral-anchored)"
author: epsilon@cph.cdd.cnos
---

# CDD Iteration — Wave-Level Findings (cph#27 + cph#28, 2026-05-19 → 2026-05-20)

## Summary

Paired-cycle wave on cph. cph#27 (R3 R-side aggregate condition-response analysis, n=60 R cycles, explore/analysis mode) and cph#28 (L-cycle recovery — contralateral-anchored, design-and-build mode) were dispatched together by δ (γ wave `/tmp/cph-wave/delta-27-28-summary.md`, both γs exited cleanly in ~9 min wall; γ identity discipline clean across the wave). The two cycles ran serially in the end — δ recommended parallel α dispatch (option 1) but a worktree race on the shared `/root/cph` killed the first α-28 attempt; serial dispatch (α-27 → β-27 merge → α-28 on post-cph#27 main) was the safe fallback. Both cycles APPROVED round 1; zero RC rounds; zero binding findings across the wave; both auto-closed via `Closes #N` in β merges (`dcf688b` for cph#27, `b93b8ad` for cph#28).

**Empirical headline (for context — not a protocol-iteration finding).** R3 produced "partial GO on R-side" — H1 ankle ROM ↓ and H2 lumbar bending range ↑ as subject-paired BH-significant findings (Wilcoxon r_rb = ±1.0 on the four BH-sig features at n=10 subjects); R1 deliberately held at REVISE per cph#27's non-goals binding. cph#28 then lifted R1 REVISE → **GO with bounded scope (path (a) inferred bilateral)** — the AC5 mechanical precondition fired (R 60/60 = 100%, L 57/60 = 95%, L count = 57); the bounded-scope qualifier ("consistent with an asymmetric coordination signature") propagated verbatim across PROJECT/ROADMAP/CHANGELOG/field-report-01. The empirical state is α/β's domain; ε records it here as orienting context.

ε's role on this wave is the wave-level reviewer (parallel to β at cycle level — collapsed onto δ-as-agent for this single-session ε run per `cdd/epsilon/SKILL.md §2` small-protocol regime, identity attributed as `epsilon@cph.cdd.cnos`).

## Findings

| # | Axis | Severity | Description | Evidence | Disposition |
|---|------|----------|-------------|----------|-------------|
| F1 | cdd-protocol-gap | B | **Parallel α dispatch on a shared working tree is a worktree race.** δ recommended option (1) parallel dispatch in `delta-27-28-summary.md §Next δ action`. The actual execution went sequential after the first α-28 attempt was killed: α-28 had attempted `git worktree add /tmp/cph-alpha-28` to isolate its working copy under concurrent α activity on shared `/root/cph`; δ intervention killed it before the worktree-add setup completed. Serial dispatch (α-27 → β-27 merge → α-28) was the safe fallback. cph#28 still merged cleanly but ~2h of α work was serialized rather than parallel. The fix is δ-side (pre-create per-α `git worktree add` directories before dispatching), not α/β-side — the role skills already prescribe per-role identity + branch isolation; the race is at the harness/filesystem level. | `/tmp/cph-wave/delta-27-28-summary.md §Next δ action` (option-1 recommendation, advisory); user-confirmed wave-prompt note ("my (δ) intervention killed it before it completed setup; serial dispatch was the safe fallback"); only one α-28 attempt visible on `origin/cycle/l-cycle-recovery` (the successful sequential one at `0235b91`); no traces of `/tmp/cph-alpha-28/` worktree on disk at ε review time | **next-MCA: cnos#384** filed 2026-05-20 (`usurobor/cnos#384`) — "operator(dispatch): parallel α dispatch on shared working tree requires per-role pre-created worktree." Same finding class as wave-2026-05-12 + wave-2026-05-18 corner-of-wave-primitive findings (the wave primitive has corners the cycle-level protocol doesn't cover yet). Resolution surface: new "Parallel dispatch" subsection in `cdd/operator/SKILL.md §3` with pre-creation + cleanup contract; cross-ref to `cdd/beta/SKILL.md` row 1 worktree-identity-isolation rule. |
| F2 | cdd-tooling-gap | C | **Read-before-Write retry inflation in long incremental-section authoring cycles.** `cdd/alpha/SKILL.md §2.5` requires incremental section authoring (each self-coherence section committed and pushed separately to survive stream timeouts). The harness Write/Edit tools require a prior Read of the file in the same session. Across cph#28's 8 self-coherence sections + multi-section field-report-01 rewrite + status-surface updates on PROJECT/ROADMAP/CHANGELOG + regenerated `analysis/feature-summary-zeroth-pilot.md`, each successive write surfaced "must Read before Write" tool errors. α-28 hit ≥3 visible TOOL_ERRs, contributing to elevated turn count (~74) and cost (~$10.29). α-28 explicitly framed the §2.5 discipline as net-correct ("the tradeoff between Read-cache invalidation cost and stream-timeout safety is implicit in the current rule; the friction is observable but the discipline is on net correct for the timeout property it protects"). | `.cdr/unreleased/28/alpha-closeout.md §"Pattern: Read-before-Write retry friction in long incremental-section authoring cycles"`; user-confirmed wave-prompt note ("turn count to 74 and cost to $10.29"); same finding class as cph#26 P3 executed-notebook Read-tool limit | **no-patch (harness-level friction; not cdd-rule-able).** The `cdd/alpha/SKILL.md §2.5` incremental-write discipline is correct for the stream-timeout property it protects; α-28's own framing concurs. The friction is observable as a turn-count amplifier but not addressable in cdd skills — Read-cache semantics live below the role-skill surface. Recorded as a known cost pattern; future cycles with many sequential file touches will see similar inflation. `cdd/gamma/SKILL.md §Cycle scope sizing` could use turn-count as an observable sizing signal but ε does not patch this — the existing five-factor heuristic already absorbs cycles of this shape via factor (c) lifecycle span. |
| F3 | cdd-protocol-gap (positive) | info | **Design-and-build with binary direction-choice criterion at AC1 works cleanly.** cph#28's issue body specified the (a)/(b) direction-choice as a binary tool-and-data-reachability check ("If the source TRC files are reachable and OpenSim IK is available, path (b) gives stronger evidence and should be preferred. Otherwise, path (a) gives a contralateral-inferred surface that is honest about its inference"). γ-28 scaffold §Design carried the criterion verbatim; α-28 evaluated at intake (TRC reachable PASS, OpenSim absent FAIL → path (a) mechanically); β-28 re-ran the same reachability probe and confirmed independently. The criterion's evaluation was observable from α dispatch and re-verifiable from β. | cph#28 issue body §"Decision criterion for (a) vs (b)"; `.cdr/unreleased/28/gamma-scaffold.md §Design`; `.cdr/unreleased/28/self-coherence.md §AC1` + `.cdr/unreleased/28/alpha-closeout.md §"Pattern: path-(b) infeasibility evaluated at α intake → mechanical fall-through to path (a)"`; `.cdr/unreleased/28/beta-review.md §AC1` (re-ran reachability probe at β intake) | **no-patch (positive precedent).** Existing `cdd/issue/SKILL.md §Mode declaration` (design-and-build with α-side direction-choice at AC1) absorbed this cleanly; γ-scaffold's load-bearing carry of the binary criterion was the discriminating discipline. Whether to formalize "binary direction-choice criteria for design-and-build issues" as a `cdd/issue/SKILL.md` recommendation needs another 2–3 examples before pattern strength supports a rule. Recorded as a strength signal — first cph design-and-build cycle with a binary direction-choice criterion that resolved without operator intervention or scope renegotiation. |
| F4 | cdd-skill-gap (positive) | info | **Cross-cycle binding by surface-omission + canonical-phrase + AC7 cross-doc-consistency oracle.** cph#27 enforced its R1 binding (cph#27 cannot lift R1; only cph#28 can) by leaving `ROADMAP.md §Phase R1` unedited while declaring the binding in prose around R3's verdict on the surrounding status surfaces; β-27 verified by `git diff origin/main..HEAD -- PROJECT.md ROADMAP.md CHANGELOG.md \| grep -E '^\+.*R1.*GO\|^\-.*R1.*REVISE'` returning empty. cph#28 propagated "GO with bounded scope (path (a) inferred bilateral)" verbatim across PROJECT/ROADMAP/CHANGELOG/field-report-01 with the AC7 cross-doc consistency oracle as the verifier. The same pattern class fired on both sides of the wave (cph#27 by surface omission, cph#28 by canonical-phrase propagation), and γ-side scaffolding carried the binding load-bearingly into both α and β dispatch surfaces. | `.cdr/unreleased/27/alpha-closeout.md §"Pattern: cross-cycle binding enforced by surface omission rather than overwrite"`; `.cdr/unreleased/27/beta-review.md §AC6` (R1 binding diff-sweep oracle); `.cdr/unreleased/28/alpha-closeout.md §"Pattern: R1 mechanical-gate transition with bounded-scope qualifier propagated uniformly across status surfaces"`; `.cdr/unreleased/28/beta-review.md §"R1 transition discipline"` + §AC7 cross-doc consistency check | **no-patch (positive precedent, two-cycle pattern).** Existing `cdd/gamma/SKILL.md` cross-cycle-coordination scaffold + `cdd/CDD.md §Tracking` (β diff-sweep oracle) handled both directions. Whether to name "canonical-phrase + cross-doc-consistency-oracle" as a recurring pattern in `cdd/CDD.md §Tracking` — vs. leaving it as per-cycle γ-scaffold convention — is plausible but the two-cycle data is thin. Recorded as a strength signal; γ-scaffold §Cross-cycle coordination is the load-bearing surface for both directions of the pattern. |
| F5 | cdd-skill-gap (positive) | info | **Analysis-cycle test surface = reproducibility property (deterministic seed + single-script run + stdout-as-table + β re-run-and-diff).** α-27 marked `cdd/alpha/SKILL.md §2.6` row 13 ("test assertion count from runner output") as N/A with named substitution oracle (`python3 analysis/r3_subject_aggregate_tests.py` reproduces every cell of the AC1/AC1c/AC2/AC3/Appendix-A tables deterministically; seed=20260519; B=10,000 percentile bootstrap). β-27 independently re-ran the script and confirmed verbatim reproduction including bootstrap CIs. | `.cdr/unreleased/27/alpha-closeout.md §"Pattern: analysis-cycle test surface is the reproducibility property, not assertion count"`; `.cdr/unreleased/27/self-coherence.md §CDD-Trace step 4` + §Pre-review gate row 13; `.cdr/unreleased/27/beta-review.md §"AC2 — β independent oracle"` (spot-checked 3 features; W, r_rb, CIs, p_raw, p_BH each reproduced) | **no-patch (positive precedent).** Existing `cdd/alpha/SKILL.md §2.6` row 13 admits N/A; the cycle exercised the rule cleanly. The substitution-oracle shape (seed + script + stdout-diff against report markdown) is portable to future analysis cycles. Whether to name the substitution-oracle pattern explicitly in row 13 — vs. leaving the row generic and relying on per-cycle α discipline — is open but ε judges the existing row generic-enough. |
| F6 | cdd-skill-gap | C | **Recurring schema-doc drift on feature-column-add (sibling-surface drift, F-class).** cph#28 added `detection_method` column to the per-cycle feature table (emitted by `scripts/features.py::extract_features`, values `"measured" \| "inferred_contralateral" \| "inferred_contralateral_partial"`) but did not co-update `analysis/feature-table-schema.md` or `analysis/features.md`. The same pattern class fired in cph#26 fix-round 1 (sibling-surface drift, F-class). The cdd-rule (`cdd/alpha/SKILL.md §2.6` row 6 — "schema / shape audit when contracts changed") fired correctly both times: α-28's row 6 audit ran and named the gap as known debt D1, then α deferred the resolution explicitly ("to keep scope on the L-recovery + R1 transition"). β-28 did not flag as RC (correctly — outside the binding contract). | `.cdr/unreleased/28/alpha-closeout.md` §"D1 — feature-table schema docs do not yet document the `detection_method` column"; `.cdr/unreleased/28/self-coherence.md §Debt D1` + §Pre-review gate row 6 "Partial PASS — known debt named"; ε re-verified: `grep detection_method analysis/feature-table-schema.md analysis/features.md` returns no hits (the column is in code + cycle-dir + status surfaces but not in either schema doc) | **next-MCA: cph#29** filed 2026-05-20 (`usurobor/cph#29`) — "feature-table schema doc-drift on detection_method column (post-cph#28); recurring sibling-surface pattern." Resolution surface is project-side (cph schema docs); cdd-rule already exists (`alpha/SKILL.md §2.6` row 6) and applied correctly. The recurrence is a project-level enforcement question (no co-update gate for feature-column-adds), not a cdd-protocol-rule revision. ε does not author the schema-doc row directly — preserves α/β independence for the cph follow-on cycle. |
| F7 | (drop) | — | δ Monitor anomaly scan returned 3 benign hits for γ-27 and 6 for γ-28: CDD.md 25k-token Read cap exceeded → retried with offset/limit windows; first `/tmp` permission prompt → retried after `acceptEdits` grant; `git rev-parse cycle/{name}` non-existent-branch probe expected to return "fatal:" before γ creates the branch. All recovered intermediate state; no aborts, no write failures. | `/tmp/cph-wave/delta-27-28-summary.md §Anomaly scan` | **drop — known/benign harness friction; δ-summary's own framing is the durable record.** Same class as wave-2026-05-18.md F6 (δ Monitor filter iteration). Not cdd-rule-able; γ's retry discipline at `Read` cap is already prescribed by `CDD.md §1.4` large-file authoring rule (read in offset/limit windows). |
| F8 | (operator-env) | info | **Legacy `cycle/{8,9,10,13,14}` branches remain on `origin`.** `CDD.md §1.4` Phase 5b step 17 ("δ deletes merged remote branches; γ may execute directly if δ unavailable") was not executed for those pre-cph#27/28-discipline cycles. cph#27 and cph#28 branches were both correctly cleaned: `git ls-remote --heads origin` returns `cycle/{8,9,10,13,14}` but NOT `cycle/{r3-rside-aggregate-analysis,l-cycle-recovery}` — β-side post-merge deletion worked for the 27/28 wave. | `git ls-remote --heads origin` at ε review time: `cycle/8 cycle/9 cycle/10 cycle/13 cycle/14` present; `cycle/r3-rside-aggregate-analysis` + `cycle/l-cycle-recovery` absent (β post-merge delete confirmed) | **no-patch (operator-env follow-on; out of ε scope).** Operator may execute `git push origin --delete cycle/8 cycle/9 cycle/10 cycle/13 cycle/14` when convenient. The cdd-rule already exists and the new-wave discipline holds; legacy branches are a one-shot cleanup, not a protocol gap. Recorded for operator visibility only. |

## Drop list (one-off / resolved / out-of-scope; named per `epsilon/SKILL.md §1` "drop")

For completeness — close-out artifacts surfaced these patterns; ε judges each as one-off / resolved-intra-cycle / non-protocol:

- **cph#27 P1 (issue-body shorthand: schema-shape count vs testable-population count, e.g. "35 columns" vs "25 testable features").** α-27 surfaced the interpretation in self-coherence §Method picks §M5 + field-report-03 §"Aggregation surface (AC1)"; β-27 re-verified via independent script run and confirmed the 700-cell oracle holds under either reading. **Drop reason:** one-off (n=1 surface); α's §Method picks discipline absorbed it without β intervention. Pattern strength insufficient for a `cdd/issue/SKILL.md` AC-discipline rule. If a 2nd cph cycle hits the same conflation, ε re-evaluates.
- **cph#28 D5 / β-28 O1 (three cosmetic remnants of pre-cph#28 REVISE-era text in `reports/field-report-01-existing-data-zeroth-pilot.md`).** Falsification §Score paragraph; Go/No-Go §Criteria Evaluation table; §"If REVISE (current recommendation)" subheader. **Drop reason:** binding contract surfaces (Recommendation, per-condition table row 3, Overview / Segmentation-Status cycle counts, path-(a) honesty caveat) are updated coherently. β-28 explicitly classified as non-binding. Operator-side or docs-tidy cycle may refresh; not a protocol gap.
- **Pseudoreplication-discipline-as-enforcement (named in user-prompt out-of-scope follow-ups).** α-27 self-coherence §Self-check item 6 already names "AC1 mandated per-subject aggregation, not per-cycle. α aggregated by (subject, condition) before any cross-subject test; n=10 paired deltas drive every Wilcoxon test, never n=60 cycles." The rule is operative as α-discipline + β-oracle. **Drop reason:** the discipline worked cleanly this cycle without an enforcement mechanism. Converting it to a project-side gate (e.g., a pre-commit grep oracle on aggregation calls) is a future cph tooling-or-CI question, not a cdd-protocol or wave-iteration scope.
- **cph#28 D2 (coverage threshold 0.80 and refinement-disabled empirical choices).** Named in α-28 self-coherence + closeout as tunable project-level constants. **Drop reason:** project-side empirical choice, not cdd-protocol. The two constants are documented in `scripts/segmentation_contralateral.py` module docstring with empirical rationale; re-evaluation belongs to whatever cycle ports the wrapper to a different archive.
- **cph#28 D3 (no programmatic test for `segment_trial_with_contralateral_l`).** Named in α-28 self-coherence + closeout. **Drop reason:** matches cph's existing "smoke via notebook + ad-hoc calibration" convention (cph#26 pattern); converting to unit-test discipline is a project-side choice for a future cph cycle.
- **cph#28 D4 (R-vs-L bilateral analysis downstream cycle).** Named in α-28 closeout as the natural next R3 sub-action. **Drop reason:** downstream empirical work, not a cdd-protocol gap.
- **cph#27 N1 (pelvis_tilt_range_deg held below AC4 line).** β-27 surfaced as non-binding observation; α's call within AC4's ≥3 floor; mechanism overlaps Candidate 1. **Drop reason:** one-off α judgment call, not a pattern.
- **cph#27 N2 (inline `df_to_markdown()` helper avoiding `tabulate` dep).** β-27 surfaced as non-binding observation; acceptable for single-purpose reproducibility harness. **Drop reason:** project-side dependency-management choice, not cdd-protocol.

## Protocol health signal

The wave pattern works on cph: a paired-cycle wave (cph#27 + cph#28) with strict cross-cycle binding (R1 ↔ cph#28 only; cph#27 cannot lift R1) closed cleanly with zero binding findings and zero RC rounds across both sides. γ-scaffold-side cross-cycle-coordination + α-side method-picks discipline + β-side AC oracle independence + cross-doc consistency check on canonical phrases were the load-bearing disciplines. The structural finding (F1 — parallel α dispatch on shared working tree) is the same class as wave-2026-05-12 findings #1–#3 and wave-2026-05-18 F1: the wave primitive has corners the original cycle-level protocol doesn't cover yet, and each new wave closes one corner.

cph-side discipline has now sustained two waves under triadic CDD (the cdr-refactor wave 2026-05-18 + this paired-cycle wave 2026-05-20). Both went APPROVE round 1 across every sub/cycle; both produced one cnos protocol-MCI (cnos#378 then cnos#384); both produced at most one cph follow-on issue (none on wave-2026-05-18; cph#29 on this wave). The two-wave history is internally consistent with the small-protocol-regime ε-collapse-onto-δ pattern (`epsilon/SKILL.md §2`) — protocol-iteration volume does not yet justify a dedicated ε agent.

The empirical headline (R1 REVISE → GO with bounded scope; R3 partial GO on R-side) is significant for the cph project but is α/β's domain. ε notes it for orienting context — the next cph cycle is downstream of an R1 GO state, which changes the *project's* substantive next move (bilateral analysis becomes evaluable) but does not change cdd-protocol obligations.

## Aggregator update

cph repo has a `.cdr/iterations/` tree (created by wave-2026-05-18.md). This wave file (`wave-2026-05-20.md`) is the second entry; no `INDEX.md` exists yet on cph. The cnos-side INDEX (`cnos:.cdd/iterations/INDEX.md`) is the canonical cdd-protocol aggregator and remains the source-of-truth for cross-tenant findings. F1's `next-MCA` issue `cnos#384` will be reflected in the cnos-side INDEX when its cycle closes; F6's `next-MCA` issue `cph#29` is project-side and does not need a cnos-INDEX entry. If future cph waves accumulate findings at a rate justifying a local INDEX, that's a project-level decision for the cph operator — two wave files is below the threshold.

## Cross-references

- Per-cycle artifacts:
  - cph#27 (R3 R-side aggregate analysis): `.cdr/unreleased/27/{gamma-scaffold,self-coherence,beta-review,alpha-closeout,beta-closeout}.md`
  - cph#28 (L-cycle recovery): `.cdr/unreleased/28/{gamma-scaffold,self-coherence,beta-review,alpha-closeout,beta-closeout}.md`
- δ wave summary: `/tmp/cph-wave/delta-27-28-summary.md` (off-tree, δ-as-agent workspace)
- Empirical artifacts produced by the wave:
  - `reports/field-report-03-construct-evaluation.md` (cph#27 output; R3 partial GO on R-side)
  - `reports/field-report-01-existing-data-zeroth-pilot.md` (rewritten by cph#28 to reflect post-recovery state and R1 GO transition)
- Filed issues (this wave):
  - `usurobor/cnos#384` — operator(dispatch): parallel α dispatch on shared working tree requires per-role pre-created worktree (F1)
  - `usurobor/cph#29` — feature-table schema doc-drift on `detection_method` column (F6)
- Prior wave-iteration: `.cdr/iterations/wave-2026-05-18.md` (format precedent; cdr-refactor wave master cph#11)
- ε skill: `/tmp/cnos/src/packages/cnos.cdd/skills/cdd/epsilon/SKILL.md` (§1 ε's cdd-side scope; §2 ε ↔ δ relationship)
- post-release skill (`cdd-iteration.md` per-finding shape): `/tmp/cnos/src/packages/cnos.cdd/skills/cdd/post-release/SKILL.md §5.6b`

---
_End of wave iteration._
