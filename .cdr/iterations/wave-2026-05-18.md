---
type: cdd-iteration
scope: wave-level (cross-cycle)
date: 2026-05-18
source: "cdr-refactor wave (master cph#11; subs cph#12/#13/#14/#15)"
author: epsilon@cph.cdd.cnos
---

# CDD Iteration — Wave-Level Findings (cdr-refactor 2026-05-18)

## Summary

First post-rename wave on `usurobor/cph` (renamed 2026-05-18 from `usurobor/gait-support-paths`). Four-sub split per the five-factor heuristic against master cph#11 (10 ACs); δ-as-agent ran the wave end-to-end under §5.2 mode (γ=δ collapse per `cdd/operator/SKILL.md` §5.2). Every sub APPROVED round 1; zero β findings across the wave; zero fix-rounds. Wave produced one actionable protocol-gap finding (rule 3.11b discoverability under §5.2 wave-mode) and four no-patch observations (three positive precedents + one operator-environment debt).

ε's role on this wave is the wave-level reviewer (parallel to β at cycle level — collapsed onto δ-as-agent for this single-session ε run per `cdd/epsilon/SKILL.md` §2 small-protocol regime, identity attributed as `epsilon@cph.cdd.cnos`).

## Findings

| # | Axis | Severity | Description | Evidence | Disposition |
|---|------|----------|-------------|----------|-------------|
| F1 | cdd-protocol-gap | B | Rule 3.11b "exemption discoverability" has no clause for §5.2 wave-mode. Wave-manifest at `.cdd/waves/{wave-id}/manifest.md` carries the γ-artifact-of-record duty (γ=δ exemption, pinned paths, standing permissions, timeout budgets), but rule 3.11b literally requires sub-issue-body discoverability — three different substantive β reads across the wave's four subs is the structural fingerprint. Paired α-side: `alpha/SKILL.md` §2.6 pre-review gate has no row for γ-side artifact presence, so α cannot pre-empt the check at its own gate. | wave-closeout §"Cross-sub findings & cycle iteration" item 1; receipt §5 escalation 2 + §6 known-gaps row 1; cph#12 β-review §3.11b L133–158; cph#13 β-review §Contract Integrity row 11; cph#14 β-review §2.5 L187–195; cph#15 β-review §2.0.0 row L29 + β-closeout L55 (formal γ recommendation); cph#12 α-closeout L38–40; cph#14 α-closeout L46–51 | **next-MCA: cnos#378** filed 2026-05-18 (`usurobor/cnos#378`) — both axes consolidated (3.11b discoverability under §5.2 + α §2.6 row). Design call (amend rule 3.11b vs require body section) deferred to cnos-side γ. |
| F2 | (positive) | info | Wave manifest forward-reference contract worked cleanly. Sub C's `targets/hypothesis.tsc` referenced Sub A's authored files (README.md, CDR.md, hypothesis doc, seven-ways article) using the manifest's pinned-paths table; no `gamma-coordination.md` write needed across the wave; no rebase coordination cost. | wave-manifest "Pinned file paths (forward-reference contract)" §; cph#14 α-closeout L44; cph#14 β-closeout L76–77 ("the wave-manifest forward-reference contract worked"); cph#14 β-review L88 | **no-patch (positive precedent).** Pattern is already encoded in the wave-manifest skill surface (`operator/SKILL.md` §10.2 manifest format with pinned-paths table). Recorded here as the first wave under cph that exercised forward-reference contracts under parallelizable-but-serially-executed subs. No protocol patch warranted; the contract worked as designed. |
| F3 | (positive) | info | α §2.3 intra-doc repetition rule fired twice as α self-correction during self-coherence authoring — cph#14 grep count 6→4 (commit `1dde070`), cph#15 owner-reference count (commit `6a430a3`). Both caught before α signaled review-readiness. Cross-tenant transfer from tsc anchors (tsc#266 F3/F3-bis) held without re-derivation. | cph#14 α-closeout L22, L30, L61–62; cph#14 β-review L199 (independent β recount confirmed 4); cph#15 α-closeout L27–28 | **no-patch (positive precedent).** Existing α §2.3 rule transferred cleanly to a new tenant on its first wave. Recorded as a strength signal; no skill patch needed. |
| F4 | cdd-skill-gap | D | β-side worktree-validation handling pattern. β set up an isolated merge-test worktree with `extensions.worktreeConfig=true` + per-worktree identity config (per `beta/SKILL.md` row 1 cycle #301 incident), torn down post-test with no leakage to shared `git config user.email`. Same pattern in cph#12 and cph#14; recovery clean in both. | cph#12 β-closeout L33 ("identity did not leak to the shared repo config (per β/SKILL.md row 1 cycle #301 incident)"); cph#14 β-closeout L39 ("identity unaffected on the shared repo config — confirmed post-cleanup") | **no-patch (skill already exists and applied correctly).** The β skill (cycle #301 worktree-identity isolation) transferred to cph cleanly. δ's receipt §5 escalation 4 framed this as "probable β skill local-to-cph or a worktree-strategy edge case"; ε reads it as the existing β skill working as designed. No protocol gap; record as successful transfer. |
| F5 | (operator-env) | info | `CHANGELOG.md` `0.1.0-cdr` baseline carries `C_Σ: pending — coh unavailable` placeholders (four literal occurrences). First mechanical `coh --mode mechanical` run is operator-environment-dependent (TSC CLI not on PATH in the dispatch env). Sub C's `scripts/measure-coherence.sh` AC6 fails clearly when `coh` is missing — the script-side gate is correct. | cph#14 α-closeout L26, L30; cph#14 β-review L26–34, L199 (β independent grep confirms 4 occurrences at lines 13, 27, 46, 52); cph#14 β-closeout L63–68 (β concurs as named debt); wave-manifest standing permissions "Run `coh` (TSC CLI) in CI: best-effort" | **no-patch (operator gate; not a protocol gap).** Named as an out-of-scope follow-up in the wave-closeout. First mechanical run is operator-owned and lands when `coh` is on PATH; the placeholder cadence is honest and load-bearing. No cdd-protocol surface to patch. |
| F6 | (drop) | — | δ-side `Monitor` filter iteration during wave dispatch (grep-OOM on JSONL → broad python pattern → anchored v3 at `[branch hash]` push-line + merge-strategy phrase, final at `/tmp/cph-wave/scripts/wave-filter.py`). | receipt §3 ("Monitor filter iteration"); not surfaced in any per-sub close-out (δ-tooling, not cycle artifact) | **drop — δ-tooling iteration, not protocol gap.** δ explicitly framed it this way in its own receipt; ε concurs. Filter is durable for future cph waves under this configuration; lives in the δ-as-agent's `/tmp/` workspace and does not need a CDD-side patch. The receipt's explicit framing is the durable record. |

## Drop list (one-off / resolved / out-of-scope; named per `epsilon/SKILL.md` §1 "drop")

For completeness — close-out artifacts surfaced these patterns; ε judges each as one-off / resolved-intra-wave / non-protocol:

- **No cph-local `eng/*` skill tree** (recurring across cph#12/13/14/15 alpha-closeouts as "fourth consecutive wave-cycle"). **Drop reason:** cph-side activation incompleteness, not cnos protocol. Future cph cycles either install the `eng/*` skill tree as a local activation step or accept its absence; not ε's scope under "do not modify cnos for cph-local activation gaps."
- **PROJECT.md stale file references** (Sub 12 β-closeout L194 named `04-existing-data-comes-first.md` and `06-what-broke.md` vs actual `04-friends-are-a-pre-pilot.md` and `05-what-broke.md`). **Drop reason:** resolved intra-wave by Sub C's PROJECT.md repartition.
- **Dual source-of-truth tables transient** (Sub 12 β-closeout L70 — README's 9-row vs PROJECT.md's 10-row). **Drop reason:** resolved intra-wave by Sub C's PROJECT.md shrink to operational status only.
- **AC8 oracle-paste case-sensitivity** (cph#15 β-review N1 — α pasted literal oracle without `-i`; reported output requires `-i`). **Drop reason:** one-off; β surfaced as observation N1, not finding; α's reproduction path is in the same artifact's §Self-check. Pattern strength is insufficient (1× across the wave) to warrant an α §2.6 row.
- **β did not invoke release/SKILL.md §2.5a or §2.5b** (Sub 12 β-closeout L70 item 6). **Drop reason:** wave-internal disconnect (no tag, no version bump per wave manifest's operator-gate); §2.5b applied by analogy via cph#15 β-closeout L53 ("merge commit *is* the disconnect signal for this sub"). No protocol gap.

## Protocol health signal

The wave pattern works on cph. δ-as-agent end-to-end under §5.2 on a freshly-renamed repo produced 4×APPROVE round 1, zero fix-rounds, $30.61 total cost, ~89 minutes cycles wall — within the 1500/900/600s α/β budgets that the wave-2026-05-12 finding #4 (`/tmp/cnos/.cdd/iterations/wave-2026-05-12.md`) hardened. Wave-mode infrastructure (manifest format, status table, wave-closeout, δ dispatch template per `operator/SKILL.md` §10) transferred from cnos to cph without protocol-level friction.

The structural finding (F1) is the same class as wave-2026-05-12 findings #1–#3: the wave primitive has corners the original cycle-level protocol doesn't cover yet. wave-2026-05-12 closed the artifact-durability and dispatchability corners; this wave (cdr-refactor 2026-05-18) is closing the cross-rule-composition corner — how rule 3.11b composes with §5.2 wave-mode in a non-cnos tenant.

## Aggregator update

cph repo has no pre-existing `.cdd/iterations/INDEX.md` — this wave is the first wave-level iteration artifact on cph. The cnos-side INDEX (`cnos:.cdd/iterations/INDEX.md`) is the canonical cdd-protocol aggregator and remains the source-of-truth for cross-tenant findings. F1's `next-MCA` issue `cnos#378` will be reflected in the cnos-side INDEX when its cycle closes. No cph-local INDEX is created by this wave; if future cph waves accumulate findings at a rate justifying a local INDEX, that's a project-level decision for the cph operator.

## Cross-references

- Wave manifest: `usurobor/cph:.cdd/waves/cdr-refactor-2026-05-18/manifest.md`
- Wave closeout: `usurobor/cph:.cdd/waves/cdr-refactor-2026-05-18/wave-closeout.md`
- δ-as-agent receipt: `usurobor/cph:.cdd/waves/cdr-refactor-2026-05-18/receipt.md`
- Per-sub artifacts: `usurobor/cph:.cdd/unreleased/{12,13,14,15}/{self-coherence,beta-review,alpha-closeout,beta-closeout}.md`
- Filed issue: `usurobor/cnos#378` (rule 3.11b §5.2 wave-mode discoverability)
- Prior wave-iteration (cnos): `usurobor/cnos:.cdd/iterations/wave-2026-05-12.md` (format precedent)
- ε skill: `usurobor/cnos:src/packages/cnos.cdd/skills/cdd/epsilon/SKILL.md`

---
_End of wave iteration._
