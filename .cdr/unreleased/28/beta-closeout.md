<!-- sections: [Merge evidence, AC verification table, Findings, Files merged, R1 post-merge state, Partner cycle note] -->
<!-- completed: [Merge evidence, AC verification table, Findings, Files merged, R1 post-merge state, Partner cycle note] -->

# β closeout — cph#28 — L-cycle recovery

## Merge evidence

- **Merge commit SHA:** `b93b8ad` (`Merge cycle/l-cycle-recovery — cph#28 L-cycle recovery`)
- **Merge author:** `beta@cph.cdd.cnos`
- **Merge shape:** `git merge --no-ff` (history preserved; γ scaffold + 3 α implementation commits + β review verdict all visible in `git log`)
- **Pre-merge gate:**
  - Row 1 (identity truth): PASS — `git config user.email` = `beta@cph.cdd.cnos` throughout
  - Row 2 (canonical-skill freshness): PASS — origin/main unchanged between β intake (`3f054de`) and merge time
  - Row 3 (non-destructive merge-test): PASS — auto-merge clean in throwaway worktree, 0 unmerged paths, worktree torn down; worktree-local git config set with `--worktree` flag to avoid shared-config leak
  - Row 4 (γ artifact completeness): PASS — `.cdr/unreleased/28/gamma-scaffold.md` present at `5de740e` (`gamma@cph.cdd.cnos`)
- **Linearity:** origin/main was an ancestor of cycle/l-cycle-recovery at merge time (fast-forwardable; merged with `--no-ff` to preserve cycle history)
- **Rounds to APPROVE:** 1 (no RC rounds)

## AC verification table

| AC | Surface | Verdict |
|---|---|---|
| AC1 | Path (a) chosen; reachability probe at β intake matches α's documented criterion outcome (TRC reachable, OpenSim absent); (b)-blocker named in self-coherence + field report | PASS |
| AC2 | 57 / 60 trials yield ≥1 L cycle (95%); 57 L cycles ≫ 10 target; three sub-threshold trials documented | PASS |
| AC3 | Per-(subject, condition) L count table; 57 bilateral (subject, trial, cycle_number) pairs; `lr_asymmetry` computable on 60 non-empty rows; notebook output cross-checked | PASS |
| AC4 | R cycle count 60/60 preserved; R durations unchanged (mean 1.06 s, range 0.89–1.37 s); `detect_heel_strikes` byte-identical to post-cph#26 form; only backward-compatible default field added to `Cycle` dataclass | PASS |
| AC5 | Recovery method documented; post-recovery cycle counts updated; falsification condition 3 row updated; explicit decision "GO with bounded scope (path (a) inferred bilateral)" matching the AC5 precondition; path (a) honesty caveat operational across multiple sections | PASS |
| AC6 | PROJECT.md / ROADMAP.md / CHANGELOG.md all updated coherently with R1 GO with bounded scope; new CHANGELOG 0.3.2 entry in narrative-progress form | PASS |
| AC7 | `git diff origin/main..HEAD -- README.md docs/concepts/* docs/articles/*` → 0 lines; cross-doc empirical-state consistency confirmed | PASS |
| AC8 | No data files committed; no tracked data files; per-cycle features CSV lives outside repo per unchanged write logic | PASS |

## Findings

**Binding findings:** None.

**Non-binding observations:**

- **O1 — stale text drift in `reports/field-report-01-existing-data-zeroth-pilot.md`.** Three subsections retain pre-cph#28 prose remnants: (a) Falsification §Score paragraph still labels condition 3 as "blocked on data shape", though the per-condition table row 3 above is updated; (b) Go/No-Go §Criteria Evaluation table still shows cph#26-era L-side numbers (L=1) below a cph#28-era Recommendation (L=57, GO); (c) "Next Phase Preparation" §"If REVISE (current recommendation)" labels REVISE as current though the Recommendation is GO. Not blocking: the binding AC5 contract surfaces (Recommendation prose, per-condition table row 3, Overview/Segmentation-Status cycle counts, path (a) honesty caveat) are updated and consistent; the AC7 cross-doc consistency check passes on the surfaces it covers. Surface for downstream tidy-up.

## R1 post-merge state

- **R1 transition:** REVISE → **GO with bounded scope (path (a) inferred bilateral)**
- **Precondition check (AC5):** AC1 ≥ 80% on R (60/60 = 100%) and L (57/60 = 95%); L count = 57 ≥ 10. Precondition **met**; transition is licit.
- **Bounded scope:** all 57 L cycles are inferred (R HS + half-stride offset) and partial-clip (coverage 0.80–0.94); bilateral asymmetry features carry an inference layer that R-vs-R features do not. Downstream R3-bilateral and R4-falsification reports must apply the path (a) honesty caveat.

## Files merged from cycle/l-cycle-recovery

```
.cdr/unreleased/28/beta-review.md       (β; new)
.cdr/unreleased/28/gamma-scaffold.md    (γ; new)
.cdr/unreleased/28/self-coherence.md    (α; new)
CHANGELOG.md                            (α; 0.3.2 entry added)
PROJECT.md                              (α; empirical decision + blocker + next action + last field report)
ROADMAP.md                              (α; current state + R1/R3/R4 phase blocks)
analysis/feature-summary-zeroth-pilot.md (α; regenerated by notebook with cph#28 §)
analysis/r3_subject_aggregate_tests.py  (α; NON_FEATURE_COLS adds detection_method)
notebooks/existing-data-processing.ipynb (α; regenerated against unchanged archive)
reports/field-report-01-existing-data-zeroth-pilot.md (α; rewritten post-recovery)
scripts/build_notebook.py               (α; wires contralateral wrapper + cph#28 summary)
scripts/features.py                     (α; emits detection_method)
scripts/segmentation.py                 (α; Cycle.detection_method field, default "measured")
scripts/segmentation_contralateral.py   (α; new module, 471 lines)
```

14 files, 1665 insertions / 452 deletions per merge commit stat.

## Partner cycle note

Partner cycle cph#27 (`cycle/r3-rside-aggregate-analysis`) merged earlier on 2026-05-19 at commit `dcf688b`. The merge-order coordination question from the dispatch context (cph#28 first vs cph#27 first) is moot — cph#27 was already on origin/main when β intake began. No rebase coordination required.
