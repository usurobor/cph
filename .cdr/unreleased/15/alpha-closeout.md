# α Close-out — cycle/15 — Sub D — CDR refactor conformance sweep

**Issue:** usurobor/cph#15
**Master:** usurobor/cph#11
**Wave:** `.cdd/waves/cdr-refactor-2026-05-18/` — final terminal sub
**Mode:** docs-only (conformance sweep — verifies; authors no swept file)
**Branch (still live; δ owns disconnect at wave terminal state):** `cycle/15`
**Base SHA at α intake:** `cf240e1` (δ wave-status commit opening cycle/15 from `e22108b`)
**Implementation SHA at review-ready:** `627191e` (last α content commit before §Review-readiness)
**Review SHA at APPROVE:** `563c7e4` (α readiness-signal commit; β's review SHA)
**β verdict:** APPROVED round 1, 0 findings (4 observations N1–N4)
**β verdict commit:** `6f82dd0`
**Merge commit:** `9bf00f1` on `main`
**β close-out commit:** `275ba10`

## Cycle summary

Goal: verify master cph#11 ACs 8 (no empirical overclaim), 9 (no data policy regression), and 10-final (source-of-truth boundaries explicit) across the merged surface delivered by Subs A (cph#12, `c3c274a`), B (cph#13, `49cd888`), C (cph#14, `f6ad183`). Result: 3 ACs substantively MET, 1 round, APPROVE, 0 findings, 4 β observations (N1 AC8 oracle-paste discrepancy; N2 3.11b discoverability wave-level pattern; N3 wave-internal disconnect posture; N4 cycle health positive).

Diff against `origin/main` at the implementation SHA: 1 file changed (`.cdd/unreleased/15/self-coherence.md`, 231 lines). The narrowest cycle diff of the wave — the dispatch's hardest constraint (*do not silently rewrite charter content*) was met by `git diff --name-status origin/main..cycle/15` returning a single file, all under `.cdd/`.

α implementation ran as the seven-section incremental self-coherence chain per α §2.5 — `23a2112` §Gap → `6091f72` §Skills → `c89211a` §ACs → `b01248a` §Self-check → `6a430a3` mid-cycle intra-doc-count correction → `8b60d8f` §Debt → `627191e` §CDD-Trace → `563c7e4` §Review-readiness. Eight α commits total (seven section commits + one self-caught intra-doc count correction). No resumption, no stream timeouts, no β fix-rounds, no charter / roadmap / infra content authored or modified.

## Friction log

- ~5 min on the AC8 oracle false-positive disambiguation at `ROADMAP.md:3`. The literal issue oracle (regex `hypothesis is (now )?(validated|proven|confirmed)` plus four sibling alternations) matches the substring `Hypothesis is validated` inside the disjunctive clause `the gates by which the Coherence Path Hypothesis is validated, revised, or abandoned` — process language about the roadmap's purpose, not a positive empirical claim. Three response paths considered: (1) mechanical rephrase of L3 on cycle/15; (2) file as deferred debt and modify nothing; (3) modify the issue's oracle regex. Picked (2): (1) violates dispatch constraint *do not silently rewrite charter content* and would introduce master/Sub-B divergence (the same disjunctive phrasing appears verbatim in master cph#11 §"Definition of done"); (3) is silent rewrite of issue-as-spec. Recorded as `self-coherence.md` §Debt F1 with file + line + merge SHA + substantive disposition. β concurred (`beta-closeout.md` §Implementation Assessment: "α's choice to file this as deferred debt … was the right call").
- ~2 min on the AC8 oracle-paste case-sensitivity discrepancy. α pasted the issue's literal oracle command (`git grep -nE`, no `-i` flag) and reported its output as "1 match" at `ROADMAP.md:3`. The literal command in fact returns 0 matches (exit 1); the reported 1-match output requires the `-niE` case-insensitive variant (which α's §Self-check L156–162 peer-enumeration table correctly displays). β re-ran both forms and surfaced the discrepancy as `beta-review.md` §Notes N1 (observation, not finding): "substantively, the literal-oracle result (0 matches) is cleaner than α reported, so the AC8 verdict is unchanged (MET). The reproduction path exists in the document itself (§Self-check uses -niE)." The discrepancy survived α's pre-review gate because the gate's row-by-row check did not include "re-execute pasted oracle commands and verify pasted output is the exact command's exact output."
- ~3 min on the intra-doc owner-reference count in §Self-check. First draft of §Self-check stated `§"Where to go next" L113–119 explicitly links 5 owners`; the actual count from a fresh `grep -n` was the basis for the correction (commit `6a430a3`: "5 of 9 in §Where-to-go-next; remaining 4 named in L109"). Same class as α #14's mid-cycle correction (commit `1dde070`, polyglot re-audit grep count 6 → 4). Caught at the §Self-check / pre-review-gate boundary, committed and pushed before §Debt was authored.
- Zero friction on the AC9 oracle. `git diff --name-only 317779c^1...cf240e1 -- '*.trc' '*.mot' '*.sto' '*.c3d' '*.osim' '*.mp4' '*.mov' 'data/external/**'` returned empty in one pass; adjacent checks (`data/`, `*.ipynb`, `data/external/` tracked tree) extended coverage without altering the verdict.
- Zero friction on the AC10 path-existence loop. The 9 owners enumerable from `README.md` L97–107 all resolved on first `test -e`.
- No mid-cycle patches from β. No β fix-rounds.

## Pre-review gate observations

α §2.6 row outcomes worth surfacing:

- **Row 11 (artifact enumeration matches diff) — narrowest possible scope.** `git diff --stat origin/main..HEAD` returned a single file (`.cdd/unreleased/15/self-coherence.md`). The TSC #23 cycles #25 / #29 derivation that motivates row 11 is structurally vacuous in sweep mode — there is no scope to under-enumerate when the diff is one artifact. β re-verified independently (`beta-review.md` §V1 row 11).
- **Row 12 (caller-path trace for new modules) — N/A, sweep authors no code.** Recorded as vacuous in `self-coherence.md` §Review-readiness; β concurred. Same outcome class as cycles 12 and 13 (docs-only authoring); first cycle of the wave where the row is vacuous *because the diff carries no executable surface at all*.
- **Row 10 (CI green) — vacuous, seventh consecutive cph cycle.** `.github/workflows/` does not exist; α §2.6 row 10's "local CI unavailable" path applies; β re-verified by `ls .github/workflows 2>/dev/null` (`beta-review.md` header). Cycles 8, 9, 10, 12, 13, 14, 15 all resolve row 10 as structural absence.
- **Row 1 (rebased on current main) — held trivially under wave-mode parallel work.** Base SHA at intake (`cf240e1`) equals base SHA at review-ready observation time. No sibling cycle is open against `main` — Sub D runs alone in the wave's final position. Same transient-row drift suppression pattern α #12, α #13, α #14 named; fourth consecutive wave-cycle the class fires zero times.
- **Row 9 (polyglot re-audit) — single-language Markdown diff.** The α surface is `.cdd/unreleased/15/self-coherence.md` (Markdown). The *swept* surface (A+B+C merged content) is polyglot, but row 9 names the *diff* at α's HEAD as the audit target, and the diff is one file. Same class as cycles 12 and 13 (narrative-only Markdown diff); contrasts with cycle 14 (4-language MCA-eligible infra diff).
- **Row 14 (author email canonical) — clean.** All 8 α commits in `origin/main..HEAD` at review-ready time carry `alpha@cph.cdd.cnos`. β re-verified independently (`beta-review.md` §V1 row 14): "all 8 α commits authored as `alpha@cph.cdd.cnos`. ✓ (α's count of '7 α commits' at L204/L222 was the count *before* the readiness-signal commit `563c7e4`; counting at HEAD gives 8. Process-correct — α's count snapshot was at the moment of writing §Review-readiness, before its own commit landed.)" — observation about counting-snapshot timing, not a finding.
- **Implementation SHA convention (α §2.6 closing note, #301 F3 derivation) — fourth clean application this wave.** §Review-readiness named `627191e` (last commit before the readiness-signal commit itself) as the implementation SHA, not the readiness-signal commit's HEAD. β consumed the signal at `563c7e4` per the "let polling carry HEAD" reading; both surfaces agreed without a self-stale loop. Cycles 12, 13, 14, 15 applied the convention identically.

Row not exercised: row 13 (test assertion count from runner output) — sweep produces no runner output; oracle outputs pasted verbatim in §ACs serve the same function.

## Skill / spec gaps that survived the gate

Observable gaps that reached β rather than being closed at α's gate:

- **AC8 oracle-paste consistency (β N1).** α's §AC8 pastes the literal issue oracle command (no `-i`) and reports its output as the `-i` variant's output. The α §2.6 pre-review gate has no row equivalent to "every command pasted in the artifact has been re-executed verbatim and the pasted output matches the executed output." The discrepancy is small (the substantive verdict strengthens under the literal command) and the reproduction path is present in the same artifact's §Self-check (which displays `-niE`), so β filed it as observation not finding. Class: same family as the α #12 / #13 / #14 cph cycles where γ-artifact / shellcheck / coh-availability gaps survived the gate — gate row inventory does not yet cover the specific surface a reviewer will actually run. *Voice constraint: not recommending a row addition; observing that the surface exists.*
- **3.11b discoverability across the wave (β N2).** β re-surfaced the same observation cycle 12 / 13 / 14 surfaced: every sub in this wave operates under §5.2 (γ=δ) per the wave manifest, but no sub-issue body carries the `## Protocol exemption` section rule 3.11b strictly requires for discoverability. β accepted the exemption per wave precedent in all four subs (uniform application across cycles 12, 13, 14, 15). The substantive exemption is genuine in all four subs; the discoverability gap is consistent across the wave. Pattern unchanged across four cycles.
- **No cph-local `eng/*` skill tree.** Issue cph#15 §"Skills to load" names `eng/bash` as a Tier 3 candidate; the issue itself flags `(if present)`; `ls .cdd/` shows `iterations/`, `unreleased/`, `waves/` only — no `skills/eng/` subtree exists in cph. Same class as α #12, α #13, α #14 cycles surfaced. Fourth consecutive wave-cycle the gap surfaces; the issue body's `(if present)` hedge accommodates the absence. β accepted (`beta-review.md` §2.0 Active Skill Consistency).

## Peer-enumeration / harness-audit observations

Peer enumeration applied (α §2.3):

- **AC8 forbidden-claim families (peer set of 5).** Issue body names 5 forbidden families: hypothesis validated/proven/confirmed; seven families validated/proven/confirmed; AI can classify; support paths directly visible; gait implies identity/personality/diagnosis/pathology. Each was grepped (`self-coherence.md` §Self-check peer-enumeration table) across the full A+B+C swept surface. Hit counts: 1 / 0 / 0 / 0 / 16 — the only oracle trigger is the `ROADMAP.md:3` disjunctive (analyzed as substantively non-violating); the 16 identity/personality matches are all disclaimers / risk-naming / forward conditions, never positive claims. β re-ran the literal oracle and the broader paraphrase grep independently (`beta-review.md` §V2).
- **AC10 source-of-truth row peer set (peer set of 9).** 9 owner rows in `README.md` L97–107 enumerated by line, parsed by table, each verified by `test -e`. β re-parsed independently and confirmed 9/9 resolve, 9 distinct questions, 9 distinct owners, injective on both sides (`beta-review.md` §V2). The 9-row peer set is the *substantive* peer set; the 4-file Sub-A/B/C-authored owner set (Sub A: rows 1/2/3, Sub B: row 5, Sub C: rows 6/7/9) is the *authoring-attribution* peer set; the 2-row pre-wave-surface set (row 4 `docs/concepts/support-path.md`, row 8 `reports/`) is the *referenced-and-preserved* peer set. All three slices verified.
- **AC9 file-type peer set (peer set of 8).** Issue oracle names 8 file-type globs: `*.trc *.mot *.sto *.c3d *.osim *.mp4 *.mov data/external/**`. All 8 enumerated in one diff invocation; result empty. Adjacent peer surfaces enumerated and verified: `data/` (empty diff), `*.ipynb` (empty diff), `data/external/` tracked tree (`{README.md, opencap-lab-validation.md}` — both within issue's manifest allowlist). Sub C's `.gitignore` additions (commit `f6ad183`) tighten the surface against future regression at the gitignore layer.
- **Intra-doc repetition (α §2.3 sub-rule) — fired with a self-caught miscount.** The mid-cycle correction commit `6a430a3` is the §Self-check intra-doc owner-reference count fix (initial draft claimed `5 owners explicitly linked` in §"Where to go next" without naming where the remaining 4 owners are referenced; correction names L109 as the explicit second site, completing 9/9). Caught by re-reading the §Self-check section against the actual `README.md` content before §Debt was authored. Same class as α #14's mid-cycle correction; second consecutive wave-cycle the rule fires as a self-catch rather than as a β finding.

Harness audit (α §2.4) — N/A. The sweep modifies no parser, schema, manifest, or runtime contract; it grep-reads existing surfaces. The closest contract-shaped surface is the `README.md` source-of-truth table itself, which is documentary rather than parsed.

Polyglot re-audit (row 9) — single-language Markdown diff at α's HEAD; vacuous. The *swept content* spans Markdown, Bash, TOML, gitignore (the A+B+C polyglot surface), but each language was already polyglot-audited by its authoring sub (Sub A α #12 Markdown-only; Sub C α #14 four-language audit). Sub D's sweep re-reads these surfaces for AC8/AC9/AC10 strings, not for language-specific contract drift.

## Cross-cycle pattern observations (cph cycles 5–14 vs cycle 15)

Patterns relative to prior cph cycles, with cycles 12 (Sub A) / 13 (Sub B) / 14 (Sub C) as wave siblings:

- **APPROVE-round-1 streak holds at ten.** Cycles 5, 6, 7, 8, 9, 10, 12, 13, 14, 15 all closed as 1-round APPROVE per available close-outs and this cycle's record. No `cycle/N` in this range carries a fix-round commit chain. The streak now spans docs-only design-and-build (12, 13), docs-only sweep (15), one MCA-eligible infra cycle (14), and the prior pre-wave cycles. α-side pre-review-gate discipline transfers cleanly from narrative authoring to mechanical verification.
- **CI-absence streak at seven.** Cycles 8, 9, 10, 12, 13, 14, 15. Class unchanged from α #14 close-out: cph is structurally a docs-and-research repo where the CI rule's design assumption does not fire. Cycle 15 is the second cycle in the streak whose diff (`.cdd/` only) would not even be a candidate for a CI workflow if one existed.
- **Wave-mode parallel-cycle transient-row suppression — fourth consecutive cycle.** Cycles 12, 13, 14, 15 all held α §2.6 rows 1 (rebased on main) and 10 (CI green) without drift. The class α §2.6 rows 1 and 10 are designed to catch fired zero times across the entire wave. Sub D's parallelism class is degenerate (Sub D ran alone in the wave's final position, no siblings open against `main` during authoring) — the suppression mechanism in cycle 15 is different from cycles 12/13/14 (which had open siblings on `cycle/N` branches not on `main`) but produces the same null outcome.
- **Implementation-SHA convention — fourth clean application this wave.** Cycle 12 was the first cph cycle to apply the "implementation SHA, not readiness-signal HEAD" convention; cycles 13, 14, 15 applied it identically. Four convergences without a self-stale loop. The #301 F3 derivation transfers cleanly to cph in all three modes the wave exercised (design-and-build docs-only, MCA-eligible infra, docs-only sweep).
- **3.11b discoverability ambiguity — fourth consecutive cycle.** Cycle 12's β surfaced rule 3.11b vs wave-mode γ-artifact-path divergence as a non-binding observation; cycle 13's sub-issue body carried the γ=δ exemption inline (the intra-wave patch α #13 named); cycle 14's β consumed the same exemption signal without raising it; cycle 15's β re-surfaces the wave-level pattern as N2. Pattern: the cycle-13 intra-wave patch (issue-body `γ = δ permitted.` line) satisfied β's *consumption* but β still re-surfaced the *structural* gap in the closing cycle of the wave. Cross-cycle reading: a same-wave protocol patch at issue-body granularity can route around a β raise without closing the underlying skill-level discoverability gap.
- **Docs-only-cycle dominance.** Cycles 7, 8, 9, 12, 13, 15 docs-only vs cycle 10 (code + docs) vs cycle 14 (infra + docs). Six of the eight most recent cph cycles are docs-only. Cycle 15 is the narrowest docs-only diff in the run (one file, `.cdd/` only — pure verification artifact).
- **Self-caught mid-cycle correction pattern — second consecutive wave-cycle.** Cycle 14 commit `1dde070` corrected a §Self-check polyglot-re-audit grep count (6 → 4). Cycle 15 commit `6a430a3` corrected a §Self-check intra-doc owner-reference count (5 of 9 + remaining 4 named in L109). Both caught by α before review-readiness was signaled; both committed as standalone fix commits in the §Self-check / §Debt boundary; neither reached β as a finding. Class: the α §2.3 intra-doc-repetition sub-rule (grep before claiming counts) is firing as a self-catch in the §Self-check authoring window, not as a β-side correction. Two consecutive wave-cycles the rule has fired this way.
- **Pre-paste-execution oracle-paste gap — first cycle of the wave it surfaces.** Cycle 15's α pasted the issue's literal oracle command and reported the `-i` variant's output. The α §2.6 pre-review gate's row inventory does not currently include a "re-execute every pasted command and verify pasted output is the exact command's exact output" row. Cycles 12, 13, 14 of this wave did not paste verbatim oracle commands from the issue body in the same form — cycle 14's oracle surfaces are the script's missing-`coh` branch (executed end-to-end with pasted output) and the registry/script triple-alignment check (β re-verified). Cycle 15 is the first wave-cycle whose ACs are *defined by* literal grep oracles in the issue body, which is the structural condition for the gap to surface. Cross-cycle observation, not a recommendation.
- **Wave terminal-sub posture — first cph terminal sub of a wave.** Cycle 15 is the wave's structural backstop: A+B+C deliver content, D verifies cross-cutting invariants without authoring any swept file. β's `beta-closeout.md` §"Process observations" names the wave-internal disconnect explicitly: no tag, no version bump, no `VERSION` file, no CHANGELOG ledger row; merge commit is the disconnect signal for this sub; δ owns wave-closeout. Class: this is the first cph cycle whose merge does not itself terminate the master-issue line (master cph#11 stays open until δ wave-closeout). The α-side artifact surface (close-out → main, no directory move, no tag, no version bump) is identical to cycles 12, 13, 14 within the wave; the *master closure* surface is what's distinct, and it's δ's not α's.

## Wave position

Sub D is the fourth of four subs to close in `cdr-refactor-2026-05-18`. Sub A (cph#12) merged at `c3c274a`; Sub B (cph#13) merged at `49cd888`; Sub C (cph#14) merged at `f6ad183`; Sub D (cph#15) merged here at `9bf00f1`. All four cycles closed as 1-round APPROVE with zero β findings. Master cph#11 remains open per the wave-internal disconnect pattern — δ owns wave-closeout (writing `.cdd/waves/cdr-refactor-2026-05-18/wave-closeout.md`, deciding per-sub `.cdd/unreleased/{N}/` directory placement, and closing master cph#11). `.cdd/unreleased/15/` remains in place until δ's wave-level disconnect per β's close-out and the operator-as-γ-and-δ pattern; α does not move directories on individual sub merges within a wave, and this close-out lands on `main` directly per the dispatch's standing permissions (no tag, no release — wave-internal).

α's work on Sub D, and on the `cdr-refactor-2026-05-18` wave, ends here.
