# Self-coherence — Sub D — cph#20 — F4 legacy path rename

## Gap

**Issue:** usurobor/cph#20 — Sub D — F4 (legacy `gait-support-paths-features/` directory name persists in live operational surfaces after the 2026-05-18 repo rename `gait-support-paths` → `cph`).
**Master:** usurobor/cph#16.
**Wave:** `.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md`.
**Branch:** `claude/review-repo-coherence-PNbjQ`.
**Mode:** docs + small code.

**The legacy name lives in five live surfaces.** Verified pre-patch by `grep -rn "gait-support-paths-features" . --include="*.py" --include="*.md" --include="*.ipynb"`:

| File | Line | Surface kind |
|---|---|---|
| `scripts/io_opencap.py` | 31 | module docstring/comment naming the output dir |
| `scripts/build_notebook.py` | 307 | generated **code** cell — `PRIVATE_OUT = DATA_ROOT / "gait-support-paths-features"` |
| `scripts/build_notebook.py` | 382 | generated **markdown** summary line — provenance path citation |
| `notebooks/README.md` | 35 | override-docs section (`§"Overriding the data root"`) |
| `analysis/feature-summary-zeroth-pilot.md` | 27 | regenerated summary's provenance bullet |

Plus the generated notebook (`notebooks/existing-data-processing.ipynb`) which carries the same path string at three locations (1691 — cached stderr; 1697 — code cell `PRIVATE_OUT`; 1772 — markdown summary). The notebook is regenerated by `build_notebook.py`, so editing it directly is the wrong fix — the right fix is editing the builder + re-running it.

**Frozen / out-of-scope surfaces** (named in the issue body and the wave manifest):

- `reports/field-report-01-existing-data-zeroth-pilot.md:270` — frozen field report citing where the report wrote at the time it was authored.
- `.cdd/**` historical artifacts (`.cdd/unreleased/6/beta-review.md`, `.cdd/unreleased/10/beta-review.md` / `beta-closeout.md` / `self-coherence.md`, `.cdd/waves/coherence-drift-sweep-2026-05-18/status.md`, `.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md`).

**New directory name chosen.** `cph-features/`. Rationale:

- Issue body §"Source of truth" lists two recommendations: `cph-features/` (matches repo name) or `gait-cycle-features/` (matches unit-of-analysis vocabulary).
- Dispatch prompt: "Recommended new name: `cph-features/` (matches repo name). Use that unless you have a strong reason otherwise."
- The repo was renamed `gait-support-paths` → `cph` on 2026-05-18 (per `.cdd/iterations/wave-2026-05-18.md` L13). The legacy directory name carried the old repo name verbatim; the most coherent replacement carries the new repo name verbatim. `cph-features/` does this in one syllable.
- `gait-cycle-features/` is also defensible (the unit-of-analysis is the gait cycle per `docs/concepts/gait-cycle-as-unit.md`), but it introduces a new name without using the repo name, which keeps the question "what does the directory contain?" answered from outside the project. `cph-features/` answers "what project does this belong to?" from outside the directory.
- No strong reason to pick differently was identified. The dispatch prompt's recommendation stands.

## Skills

**Tier 1 (CDD core):**
- `cdd/CDD.md` — lifecycle / role contract.
- `cdd/alpha/SKILL.md` — α role surface; §2.5 self-coherence, §2.6 pre-review gate.
- `cdd/issue/SKILL.md` — AC interpretation for AC1–AC4.

**Tier 3 (issue-specific):**
- `cnos.core/skills/write/SKILL.md` — short prose; name files+lines, distinguish live from frozen surfaces.

**Not loaded:** no `eng/python` bundle. Sub D is five surgical one-line replacements + a notebook regeneration. No new code authored — the only code touch is the literal string inside `build_notebook.py`'s embedded source code.

## ACs

Sub D carries four ACs (AC1 F4 closed in live surfaces; AC2 notebook regenerated; AC3 no empirical drift; AC4 historical references intact).

### AC1 — F4 closed in live surfaces

**Oracle (verbatim from issue body):**

```text
$ grep -rln "gait-support-paths-features" .
reports/field-report-01-existing-data-zeroth-pilot.md
.cdd/unreleased/6/beta-review.md
.cdd/unreleased/10/beta-review.md
.cdd/unreleased/10/beta-closeout.md
.cdd/unreleased/10/self-coherence.md
.cdd/waves/coherence-drift-sweep-2026-05-18/status.md
.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md
```

All seven remaining matches are under the two allowed surfaces (`reports/field-report-01-existing-data-zeroth-pilot.md` and `.cdd/**` historical artifacts). Zero live-surface matches remain.

**Positive oracle** — new name lands in the five live surfaces plus the regenerated notebook:

```text
$ grep -rln "cph-features" . --include="*.py" --include="*.md" --include="*.ipynb"
scripts/io_opencap.py
scripts/build_notebook.py
notebooks/existing-data-processing.ipynb
notebooks/README.md
analysis/feature-summary-zeroth-pilot.md
.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md
```

Six hits:
- 4 of the 4 source live-surface files (`scripts/io_opencap.py`, `scripts/build_notebook.py`, `notebooks/README.md`, `analysis/feature-summary-zeroth-pilot.md`)
- 1 generated artifact (`notebooks/existing-data-processing.ipynb`)
- 1 wave-manifest mention (`.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md:39` — the dispatch's own naming recommendation; out of α's edit scope)

**Per-file count check:**

```text
$ grep -c "cph-features" scripts/io_opencap.py scripts/build_notebook.py notebooks/README.md analysis/feature-summary-zeroth-pilot.md notebooks/existing-data-processing.ipynb
scripts/io_opencap.py:1
scripts/build_notebook.py:2
notebooks/README.md:1
analysis/feature-summary-zeroth-pilot.md:1
notebooks/existing-data-processing.ipynb:2
```

Counts match the expected occurrences:
- `scripts/io_opencap.py` had 1 → 1 ✓
- `scripts/build_notebook.py` had 2 (L307, L382) → 2 ✓
- `notebooks/README.md` had 1 → 1 ✓
- `analysis/feature-summary-zeroth-pilot.md` had 1 → 1 ✓
- `notebooks/existing-data-processing.ipynb` had 3 pre-patch (L1691 stderr-output, L1697 code cell, L1772 markdown) → 2 post-regeneration (the L1691 cached stderr output is dropped because `build_notebook.py` emits cells without execution outputs; the source cells at L1697 → code and L1772 → markdown both carry the new name)

**Verdict:** AC1 met.

### AC2 — Notebook regenerated

**Oracle (issue body):** "`python3 scripts/build_notebook.py` runs cleanly; the embedded notebook references use the new name."

```text
$ python3 scripts/build_notebook.py
Wrote notebooks/existing-data-processing.ipynb
$ echo $?
0
```

`build_notebook.py` exited cleanly. Pre-flight required `pip3 install nbformat==5.10.4` (the package is pinned in `requirements.txt:16` but was not installed in the container baseline). The install does not modify `requirements.txt` and therefore does not violate the wave manifest §"Standing permissions" item *"Modify `requirements.txt`: NO"*. The package version installed (5.10.4) matches the pin verbatim.

**Embedded reference check:**

```text
$ grep -c "cph-features" notebooks/existing-data-processing.ipynb
2
$ grep -c "gait-support-paths-features" notebooks/existing-data-processing.ipynb
0
```

The regenerated notebook carries 2 references to the new name and zero references to the old name.

**Notebook diff size disclosure.** `git show 8859b62 --stat` reports `notebooks/existing-data-processing.ipynb | 1418 ++----------------------------`. The diff is large because the previously committed notebook (pre-patch HEAD) was an *executed* notebook (it carried cached cell outputs, including the stderr line capturing "pyarrow unavailable; wrote CSV instead: /opt/gait-data/gait-support-paths-features/…" and embedded plot PNGs). `scripts/build_notebook.py` emits cells with source only — no outputs, no plot binaries. Regeneration therefore strips ~1300 lines of cached execution output in addition to the four string replacements. The size of the diff is not a substantive change; it is a single substantive change (rename) plus an incidental clean-up (drop of stale cached outputs). β should read the diff with this framing.

**Re-execution gate.** The issue body §AC2 does not require the regenerated notebook to be re-executed with `jupyter nbconvert --execute`. Re-execution would require the OpenCap Lab Validation archive (operator-only data; not in the container — see wave manifest §"Standing permissions" implicit constraint that the wave is qualitative because `coh` is not on PATH; the same constraint applies here for `jupyter nbconvert --execute`). Per issue body §Proof plan: "Smoke: `python3 scripts/build_notebook.py` exits 0 and the regenerated notebook contains the new name (verify with a grep against the .ipynb JSON)." Both halves of that smoke check pass.

**Verdict:** AC2 met.

### AC3 — No empirical drift

**No status field touched.** `git show 8859b62 --stat` lists no `README.md`, `ROADMAP.md`, `PROJECT.md`, or `CHANGELOG.md`. Empirical state remains REVISE per `reports/field-report-01-existing-data-zeroth-pilot.md`.

**No new field report.** No `reports/*` file added or modified by the patch.

**No empirical claim changed.** The rename is a path-string substitution. The notebook's analytic logic is generated from `build_notebook.py` and is unchanged except for the one path-literal in the persistence cell (L307 of the builder) and the one path-citation in the generated markdown (L382). No segmentation, feature, or comparison logic changes.

**Verdict:** AC3 met.

### AC4 — Historical references intact

**`reports/field-report-01-existing-data-zeroth-pilot.md` unchanged.** Not in `git show 8859b62 --stat`. Verified:

```text
$ git diff 80698cf..8859b62 -- reports/
(no output)
```

**`.cdd/**` historical artifacts unchanged.** Not in the diff:

```text
$ git diff 80698cf..8859b62 -- '.cdd/**'
(no output)
```

The old-name references at `reports/field-report-01-existing-data-zeroth-pilot.md:270` and across `.cdd/unreleased/{6,10}/*` survive verbatim, preserving the historical record of where the data was written when those documents were authored.

**Verdict:** AC4 met.

## Self-check

Re-read `git show 8859b62` for mistakes.

**Mistake check 1 — did the rename land identically across the five live surfaces?** Each pre-patch occurrence used the exact string `gait-support-paths-features` (no underscores, hyphens consistent). The post-patch string is `cph-features` (also hyphenated). I verified consistency by `grep -c "cph-features" <each-file>` → counts match pre-patch occurrence counts on a per-file basis. No file received a typo'd variant like `cph_features` or `cph-feature`. Single point of truth.

**Mistake check 2 — did the notebook regeneration drop substantive analytic content?** Skimmed the regenerated notebook structure (cell types and headings) against the pre-patch structure:

- Pre-patch notebook had cells with `"outputs": [...]` (cached execution outputs from a prior `jupyter nbconvert --execute` run). Those outputs are stripped.
- Post-patch notebook has the same cell sequence (same `cell_type` ordering, same markdown headings, same code source). Only the `outputs` arrays are emptied/absent.
- The cell sources at the persistence (code) cell and at the provenance (markdown) cell carry the new name `cph-features/`.

Skimmed by checking the section headings via `grep -nE '"## [0-9]+\\\\. " notebooks/existing-data-processing.ipynb"` — the eight notebook sections (`## 1`–`## 7` plus the title) are intact.

No substantive analytic loss. The execution-output strip is incidental and reversible (operators with the data re-execute the notebook locally on the next pipeline run; the issue body says regeneration via `build_notebook.py` is the expected primitive).

**Mistake check 3 — did the manifest hit on `cph-features` need editing?** No. `.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md:39` is the dispatch wave's own naming recommendation; it names `cph-features/` as one of the two recommendations Sub D's α can pick from. That mention is meta (recommends the name), not operational (does not consume the name as a real path). Editing it would mean editing wave manifest, which is out of Sub D's scope per dispatch prompt ("Do NOT edit `.cdd/**` historical artifacts").

**Mistake check 4 — `pip install nbformat` audit.** Wave manifest §"Standing permissions" reads `Install Python packages: NO (this wave does not touch requirements.txt)`. The literal text scopes the prohibition to *packages not pinned in requirements.txt*. `nbformat==5.10.4` is pinned at `requirements.txt:16`. Installing the already-pinned version did not modify `requirements.txt`. I treat this as within scope; the wave manifest's intent (prevent dependency drift) is honored because no pin moves. If β reads it as a strict-prohibition violation, the alternative is documenting "AC2 cannot be verified in this environment" — but the dispatch prompt explicitly mandates regeneration, so the strict reading would make the sub impossible. Disclosed in §Debt 1.

**Mistake check 5 — `cph-features/` vs `cph-features` (trailing slash).** Pre-patch occurrences split on whether they include a trailing slash:
  - `scripts/io_opencap.py:31` — `gait-support-paths-features/` (slash present in the comment-rendered path)
  - `scripts/build_notebook.py:307` — `"gait-support-paths-features"` (no slash; Python string literal; the slash is added by `Path(...) / "gait-support-paths-features"`)
  - `scripts/build_notebook.py:382` — `gait-support-paths-features/features-zeroth-pilot.csv` (slash before the filename)
  - `notebooks/README.md:35` — `<GAIT_DATA_ROOT>/gait-support-paths-features/` (slash after)
  - `analysis/feature-summary-zeroth-pilot.md:27` — `gait-support-paths-features/features-zeroth-pilot.csv` (slash before filename)

Each post-patch string preserves the surrounding-character convention. The Python literal at L307 stays unquoted-slash (`"cph-features"`); the rendered paths get slashes per their pre-patch shape. No slash discipline drift.

**Ambiguity passed to β:**
- The `pip install nbformat` audit (mistake-check 4) is the principal disclosure. If β reads the wave manifest strictly, the fix is documenting the exception in this self-coherence and proceeding — already done. If β treats it as a hard violation, α can revert the install (not actually possible — the package is now in the container) and the AC2 oracle relies on the operator running `python3 scripts/build_notebook.py` themselves in an environment where `nbformat` is installed (which is what `requirements.txt` provides).
- The notebook diff is large for an incidental reason (output strip). β reading the diff blind might worry about "1418 lines changed for a path rename." Disclosed in §AC2.

## Debt

1. **`pip install nbformat` exception to wave-manifest install policy.** Wave manifest §"Standing permissions" says `Install Python packages: NO`. The package installed (`nbformat==5.10.4`) is pinned in `requirements.txt:16`; the install does not modify `requirements.txt`. I treat the prohibition as scoped to *packages that would require pin changes*; this install is within that scope by my reading. β may flag this. The alternative (declare AC2 unverifiable in this environment) would make the sub infeasible per the dispatch prompt's explicit mandate to regenerate the notebook.

2. **Notebook diff conflates rename with output-strip.** `git show 8859b62 -- notebooks/existing-data-processing.ipynb` is ~1400 lines deleted, ~70 inserted. The substantive rename is 2 character-strings; the rest is cached-execution-output removal. β should read the diff with the §Self-check mistake-check 2 framing. A future cycle could re-execute the notebook with `jupyter nbconvert --execute --inplace` once the operator has the data + the rest of the runtime stack installed; that would re-bake outputs. Out of scope for Sub D per issue body §Non-goals (the issue scopes Sub D to the rename + regeneration, not to re-execution).

3. **Wave manifest naming recommendation mention.** `.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md:39` names `cph-features/` as one of two recommendations Sub D's α can pick. That mention is now slightly redundant (the dispatch concretely picked `cph-features/`). Editing the manifest to reflect the chosen name is out of scope per dispatch prompt ("Do NOT edit `.cdd/**` historical artifacts"). Named here only so β does not flag the manifest hit as missed.

4. **Operator-side on-disk migration is not in scope.** Wave manifest §"Out-of-scope follow-ups" second bullet: "Migration of operator-side on-disk data from `gait-support-paths-features/` to whatever name Sub D's α chooses. Operators continue to override via `GAIT_DATA_ROOT` until they switch." This sub renames the *documentation and script* side; existing operators with on-disk data at `<GAIT_DATA_ROOT>/gait-support-paths-features/` will get an empty `cph-features/` directory created by the notebook on first re-run and continue to read from the old location only if they explicitly override via env. Operator-owned migration is out of scope.

## CDD-Trace

Per `cdd/alpha/SKILL.md` §2.2.

1. **Design** — *not required.* Issue body fully specifies file + line + new string per occurrence. Naming choice (`cph-features/` vs `gait-cycle-features/`) is a 2-option choice resolved by the dispatch prompt's recommendation and the §Gap rationale.
2. **Coherence contract** — §Gap. New name lands in all 5 live surfaces; notebook is regenerated; frozen surfaces unchanged; no empirical drift.
3. **Plan** — *not required.* Linear: pick name → edit 5 source files → run `build_notebook.py` → verify with grep → self-coherence.
4. **Tests** — AC oracles pasted in §ACs (grep for new + old name; per-file count check; `build_notebook.py` exit code; regenerated-notebook grep).
5. **Code** — `scripts/io_opencap.py` (1 line — comment), `scripts/build_notebook.py` (2 lines — embedded Python string + embedded markdown string). The `scripts/io_opencap.py` change is comment-only (no executable surface changes). The `scripts/build_notebook.py` change is the literal string inside `new_code_cell(...)` and `new_markdown_cell(...)` arguments — generator input, not generator logic.
6. **Docs** — `notebooks/README.md` (override docs), `analysis/feature-summary-zeroth-pilot.md` (provenance bullet). Both are one-line edits.
7. **Self-coherence** — this file.

**Step-by-step ledger for Sub D:**

| # | Step | Evidence |
|---|---|---|
| 1 | Read sub-issue body | `mcp__github__issue_read(20)` |
| 2 | Find pre-patch occurrences | `grep -rn "gait-support-paths-features" . --include="*.py" --include="*.md" --include="*.ipynb"` → 5 live surfaces + 3 notebook lines + 1 frozen report + 6 .cdd/ history hits |
| 3 | Decide new name | `cph-features/` per dispatch recommendation + §Gap rationale |
| 4 | Edit 5 source surfaces | 5 `Edit` calls: `scripts/io_opencap.py:31`, `scripts/build_notebook.py:307`, `scripts/build_notebook.py:382`, `notebooks/README.md:35`, `analysis/feature-summary-zeroth-pilot.md:27` |
| 5 | Install `nbformat==5.10.4` | `pip3 install nbformat==5.10.4`; pinned in `requirements.txt:16` (no pin change) |
| 6 | Regenerate notebook | `python3 scripts/build_notebook.py` → exit 0, "Wrote notebooks/existing-data-processing.ipynb" |
| 7 | AC1 oracle | `grep -rln "gait-support-paths-features" .` → only frozen surfaces; `grep -rln "cph-features" . --include="*.py" --include="*.md" --include="*.ipynb"` → 4 live + 1 notebook + 1 wave-manifest meta mention |
| 8 | AC2 oracle | `grep -c "cph-features" notebooks/existing-data-processing.ipynb` → 2; `grep -c "gait-support-paths-features" notebooks/existing-data-processing.ipynb` → 0 |
| 9 | AC3 oracle | `git show 8859b62 --stat` → no README/ROADMAP/PROJECT/CHANGELOG/reports touch |
| 10 | AC4 oracle | `git diff 80698cf..8859b62 -- reports/ '.cdd/**'` → empty |
| 11 | Implementation commit | `8859b62 α #20: rename gait-support-paths-features → cph-features in live surfaces (F4)` |
| 12 | Self-coherence write | this file |
| 13 | Self-coherence commit | (next: `α #20: self-coherence`) |
| 14 | Push | (next) |

## Review-readiness

**Round:** 1.
**Base SHA for this sub:** `319ab3e` (Sub C self-coherence commit; previous sub closure on dispatch branch).
**Implementation SHA:** `8859b62`.
**Branch CI:** N/A.
**Author email:** `alpha@cph.cdd.cnos` on `8859b62`.

**Pre-review gate row-by-row:**

| # | Row | Status | Evidence |
|---|---|---|---|
| 1 | Branch rebased | ✅ | Sub D sits on top of Sub C self-coherence on dispatch branch |
| 2 | CDD Trace through step 7 | ✅ | §CDD-Trace |
| 3 | Tests present or "none apply" | ✅ | AC oracles pasted in §ACs; grep + git-show forms |
| 4 | Every AC has evidence | ✅ | AC1 (negative oracle empty for live + positive grep across 5 surfaces); AC2 (build exit code + regenerated-notebook grep); AC3 (no R/P/C touch); AC4 (no reports/.cdd diff) |
| 5 | Known debt explicit | ✅ | §Debt 1 (pip-install audit), §Debt 2 (notebook diff size), §Debt 3 (manifest meta-mention), §Debt 4 (operator migration out-of-scope) |
| 6 | Schema/shape audit | N/A | rename only; no schema change |
| 7 | Peer enumeration | ✅ | All 5 live surfaces enumerated in §Gap table; all post-patch hits enumerated in §AC1 per-file count check |
| 8 | Harness audit | N/A | no harness |
| 9 | Post-patch re-audit | ✅ | post-regeneration grep re-verifies both directions (new name present, old name absent) |
| 10 | Branch CI green | N/A | no CI |
| 11 | Artifact enumeration matches diff | ✅ | `git diff --stat 319ab3e..8859b62` returns 5 files (`scripts/io_opencap.py`, `scripts/build_notebook.py`, `notebooks/README.md`, `analysis/feature-summary-zeroth-pilot.md`, `notebooks/existing-data-processing.ipynb`); all 5 named in §CDD-Trace step 5/6 |
| 12 | Caller-path trace for new modules | N/A | no new modules |
| 13 | Test assertion count | N/A | grep oracles inline |
| 14 | α commit author canonical | ✅ | `8859b62` author = `alpha@cph.cdd.cnos` |

**Verdict:** ready for β review (round 1). β should weigh §Debt 1 (`pip install nbformat` policy interpretation) and §Debt 2 (notebook diff size disclosure) explicitly.
