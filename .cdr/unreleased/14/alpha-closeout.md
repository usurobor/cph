# α Close-out — cycle/14 — Sub C — TSC targets + measure-coherence.sh + CHANGELOG baseline + PROJECT.md repartition

**Issue:** usurobor/cph#14
**Master:** usurobor/cph#11
**Wave:** `.cdd/waves/cdr-refactor-2026-05-18/`
**Mode:** MCA-eligible (TSC target-manifest registry format string `tsc-target-registry/0.1` and `measure-coherence.sh` shape pinned in master cph#11 §"Required changes" items 6–7; PROJECT.md repartition mechanical given AC-PROJECT invariant)
**Branch (still live; δ owns disconnect at wave terminal state):** `cycle/14`
**Base SHA at α intake:** `f13164d`
**Implementation SHA at review-ready:** `d77aac4` (last implementation commit before the self-coherence sequence)
**Review SHA at APPROVE:** `b4a5569` (cycle/14 head before β verdict commit)
**β verdict:** APPROVED round 1, 0 findings
**β verdict commit:** `29bc71e`
**Merge commit:** `f6ad183` on `main`
**β close-out commit:** `2eba5ee`

## Cycle summary

Goal: render master cph#11 §"Required changes" items 6 (TSC target manifests), 7 (`scripts/measure-coherence.sh`), 8 (CHANGELOG.md research coherence ledger baseline), and 9 (PROJECT.md repartition) — closing ACs 5, 6, 7, and the AC-PROJECT invariant that supports AC10. Result: 4 ACs met, 1 round, APPROVE, 0 findings.

Diff against `origin/main` at the implementation SHA: 9 files changed, 286 insertions(+), 147 deletions(-). Five new TOML files under `targets/`, one new shell entrypoint under `scripts/`, one new repo-root Markdown ledger (`CHANGELOG.md`), one repartitioned repo-root Markdown status surface (`PROJECT.md` — 172 → 44 lines), one `.gitignore` line (`.tsc/`). The self-coherence artifact (`.cdd/unreleased/14/self-coherence.md`) lands on top of the implementation chain.

α implementation ran as four scope-named commits (`39df3d5` targets → `f868caf` script → `1de1ac5` changelog → `d77aac4` PROJECT.md) followed by the seven-section self-coherence chain (`b5797af` §Gap → `0fa962a` §CDD-Trace) plus one intra-cycle self-correction (`1dde070`, grep-count `6 → 4` in §Self-check polyglot re-audit) caught and committed before the §Review-readiness signal at `b4a5569`. No resumption, no stream timeouts, no β fix-rounds.

## Friction log

- ~3 min on the per-target manifest format-string choice (`tsc-target/0.1`). Master cph#11 §"Required changes" item 6 pins the registry format string (`tsc-target-registry/0.1`) and gives a registry example, but the per-target manifest format string and shape are not pinned. Picked `tsc-target/0.1` with `{format, name, description, sources}` keys, consistent with the registry example structure; recorded as `self-coherence.md` §Debt item 3 (one-pass schema-fix surface available if the live `coh` CLI expects a different per-target schema).
- ~2 min on the `coh` flag-spelling read. Master cph#11 §"Required changes" item 7 gives the exact `coh --mode mechanical --target <name> --registry targets/registry.tsc --output .tsc/` invocation; α used it verbatim. The dispatch environment does not have `coh` installed, so only the missing-tool branch was exercised end-to-end. Recorded as `self-coherence.md` §Debt item 2 (present-`coh` branch is structurally validated, not behaviorally validated).
- ~1 min on the `.tsc/**` exclusion-layer choice. Settled on triple-layer (`.gitignore` keeps it out of the index, `sources` arrays exclude it from canonical measurement, comment-block preambles in `registry.tsc` and `repo.tsc` document the exclusion). β-noted as a strength (`beta-review.md` §Technical Review item 3 / `beta-closeout.md` §Technical Review strength 3).
- Zero friction on the AC-PROJECT seven-field invariant. The AC spec is mechanical (named-field enumeration); the rewrite was one pass; pre-diff content removed cleanly rather than gradually deprecated.
- Zero friction on the `pending — coh unavailable` cadence across CHANGELOG.md surfaces. Four occurrences (ledger row C_Σ cell, §Coherence delta C_Σ bullet, §Known limits, §Next gate); intra-doc-repetition grep run during §Self-check polyglot re-audit caught a count error (initially counted 6, actual 4) before review-readiness was signaled.
- No mid-cycle patches from β. No β fix-rounds. α §2.6 pre-review gate ran clean on first pass.

## Pre-review gate observations

α §2.6 row outcomes worth surfacing:

- **Row 9 (polyglot re-audit) — first cph cycle since this wave opened to exercise it across more than one language.** The diff touches four languages (TOML, Bash, Markdown, gitignore). Each was audited with the matching toolchain: TOML via `python3 -c "import tomllib; tomllib.load(...)"` on all five `targets/*.tsc` files; Bash via `bash -n` (clean) plus a direct run of the missing-`coh` branch (exit 127, install-instruction block printed); Markdown via table-shape and intra-doc grep checks across `CHANGELOG.md`, `PROJECT.md`, and `self-coherence.md`; gitignore via syntax-consistency check against existing entry style. Prior cycles 12 and 13 in this wave were single-language (Markdown) and the row was structurally satisfied without an audit loop. Pattern: row 9's design assumption — that the diff is polyglot — fires in MCA-eligible infra subs but does not fire in design-and-build docs subs.
- **Row 8 (harness audit) — first cph cycle this wave to have a producer/consumer pair to audit.** Producer = the five `targets/*.tsc` files (TOML manifests). Consumer = `scripts/measure-coherence.sh` (reads the registry, dispatches `coh` per target). The four `[target.*]` blocks in `registry.tsc` exactly match the four entries in the script's `TARGETS=(repo hypothesis method evidence)` array; the harness audit verified the chain end-to-end (`self-coherence.md` §Self-check "Harness audit"). β re-verified independently as the `registry.tsc` ↔ script ↔ wave-manifest triple-alignment (`beta-review.md` §2.2 row C, `beta-closeout.md` §Technical Review strength 2). Cycles 12 and 13 in this wave had no schema-bearing contract and the row was N/A.
- **Row 10 (CI green) — vacuous, sixth consecutive cph cycle.** `ls .github/workflows/` returns "No such file or directory." α recorded the explicit absence in `self-coherence.md` §Review-readiness row 10; β re-verified in `beta-review.md` §2.4. Cycles 8, 9, 10, 12, 13, 14 all resolve row 10 as structural absence rather than a green CI run.
- **Row 1 (rebased on current main) — held trivially under wave-mode parallel work, third consecutive cycle this wave.** Base SHA at intake (`f13164d`) equals base SHA at review-ready observation time. `origin/main` did not advance during α's authoring because cycle 12 had already merged at α's intake (the base `f13164d` is `δ wave status: cycle/13 (Sub B) ✅ completed`, i.e. the wave-status commit following the cycle 12 → cycle 13 merge sequence) and no sibling cycle was open against `main`. Same transient-row drift suppression pattern α #12 and α #13 named.
- **Row 11 (artifact enumeration matches diff) — held.** Every file in `git diff --stat origin/main..HEAD` (`.gitignore`, `CHANGELOG.md`, `PROJECT.md`, `scripts/measure-coherence.sh`, `targets/{evidence,hypothesis,method,registry,repo}.tsc`, `.cdd/unreleased/14/self-coherence.md`) is named in §CDD-Trace step 6 or §ACs. The TSC #23-derived row that previously surfaced as a single-file-doc-cycle triviality (α #13 close-out) fired with substance this cycle because the diff carries 9 files across 4 languages.
- **Row 12 (caller-path trace for new modules) — applied at the script-entrypoint reading.** The script is itself the operator-facing entrypoint; no programmatic non-test caller exists or is expected. The entrypoint's existence is referenced by `CDR.md` §"Required cadence" (Sub A merged surface) and by `PROJECT.md` §"Last coherence measurement" (this sub). Recorded under §CDD-Trace step 6; β concurred (`beta-review.md` §2.2 row F).
- **Row 14 (commit author email) — clean.** All 11 cycle commits in `origin/main..HEAD` at review-ready time carry `alpha@cph.cdd.cnos`; the β round-1 commit `29bc71e` carries `beta@cph.cdd.cnos`; the merge commit `f6ad183` and the β close-out commit `2eba5ee` are both `Beta <beta@cph.cdd.cnos>`. Path-(a) retroactive-reauthor surface (#287 R1 F3 derivation) was not exercised.
- **Implementation SHA convention (α §2.6 closing note, #301 F3 derivation) — third clean application this wave.** §Review-readiness named `d77aac4` (last commit before the self-coherence sequence) as the implementation SHA, not the readiness-signal commit's own HEAD. β consumed the signal at `b4a5569` (the readiness-signal commit) per the "let polling carry HEAD" reading; both surfaces agreed without a self-stale loop. Cycles 12 and 13 of this wave applied the same convention.

## Skill / spec gaps that survived the gate

Observable gaps that reached β rather than being closed at α's gate:

- **No cph-local `eng/*` skill tree.** Issue cph#14 §"Skills to load" names `eng/bash` and `eng/markdown` as Tier 3 candidates; `ls .cdd/` shows `iterations/`, `unreleased/`, and `waves/` only — no `skills/eng/` subtree exists in cph. α applied bash discipline (`#!/usr/bin/env bash`, `set -euo pipefail`, `command -v` guard, explicit exit codes, `bash -n` validation) and Markdown discipline (table-and-section style, intra-doc cross-references) directly; recorded in `self-coherence.md` §Skills Tier 2 / Tier 3. Same class of gap as α #12 and α #13 surfaced (no `eng/*` skill tree in cph) — third consecutive cycle this wave. β accepted (`beta-review.md` §2.0 Active Skill Consistency: "applied directly").
- **Per-target manifest schema is α-chosen, not master-pinned.** Master cph#11 §"Required changes" item 6 pins the registry format string (`tsc-target-registry/0.1`) and example structure, but does not pin the per-target manifest schema or format string. α chose `tsc-target/0.1` with `{format, name, description, sources}` keys consistent with the registry example. If the live `coh` CLI expects a different per-target schema, this is a one-pass schema fix. Recorded as `self-coherence.md` §Debt item 3; β concurred (`beta-closeout.md` §Implementation Assessment, §Process Observations).
- **Present-`coh` branch not behaviorally validated.** `coh` is not installed in the dispatch environment; the wave manifest's standing permissions allow this ("Run `coh` (TSC CLI) in CI: best-effort"). The missing-`coh` branch is verified end-to-end (exit 127 + install-instruction block); the present-`coh` branch is structurally validated (read line-by-line) but not exercised. Recorded as §Debt items 1 (no numeric C_Σ baseline) + 2 (flag spelling inferred). The first numeric C_Σ baseline lands the first time `coh` runs against this branch or a successor; CHANGELOG.md §Next gate names the trigger.
- **`shellcheck` not run.** Not installed in the dispatch environment. `bash -n` is clean; the script is small (≈40 executable lines) and uses standard constructs (`command -v`, `mkdir -p`, `cat <<EOF`, a `for` loop over a literal array). Recorded as §Debt item 4. Same class as the `coh`-missing gap — tool absence in the dispatch environment is an operator / standing-permission concern, not an α-side authoring gap.

## Peer-enumeration / harness-audit observations

Peer enumeration applied (α §2.3):

- **`targets/*.tsc` file family (peer set of 5).** Registry + four per-target manifests under one schema. Enumerated and verified consistent: all five parse as TOML; all four per-target manifests carry the same `{format, name, description, sources}` shape with the same `tsc-target/0.1` format string; the registry's distinct format string (`tsc-target-registry/0.1`) is the master-specified value; all four manifest paths in the registry resolve; all 33 glob entries across the four per-target manifests resolve to ≥1 match each on the merge tree. β re-verified independently (`beta-review.md` §2.0 AC5, §Technical Review strength 1).
- **Source-of-truth surface family (peer set of 4).** `README.md` (Sub A) owns the source-of-truth table; `ROADMAP.md` (Sub B) owns gates; `CHANGELOG.md` (this sub) owns the historical ledger; `PROJECT.md` (this sub) owns the live operational snapshot. Each file points to the others; no two restate each other's owned facts. README's source-of-truth rows for `CHANGELOG.md`, `PROJECT.md`, and `targets/` now resolve to real artifacts. β re-verified the reverse direction (`beta-review.md` §2.1 "Authority conflicts").
- **Empirical-state-bearing surfaces (peer set of 4).** README.md, ROADMAP.md, PROJECT.md, CHANGELOG.md each carry an empirical-state phrase. All four cite `reports/field-report-01-existing-data-zeroth-pilot.md`; the literal phrase `not validated; it is also not refuted` appears verbatim in CHANGELOG.md and PROJECT.md; all four say REVISE. No drift.
- **Intra-doc repetition (α §2.3 sub-rule) — fired with a self-caught miscount.** The literal `pending — coh unavailable` appears 4× in CHANGELOG.md (ledger row C_Σ cell, §Coherence delta C_Σ bullet, §Known limits, §Next gate). α's first §Self-check pass recorded the count as 6; the polyglot re-audit grep returned 4 lines; the discrepancy was caught and committed as a correction (`1dde070`) before §Review-readiness was signaled at `b4a5569`. β re-counted independently (`beta-review.md` §2.6 notes: "α's corrected count is right"). The α §2.3 sub-rule fired as designed — grep the doc before claiming the count.

Harness audit (α §2.4) applied:

- Producer = the five `targets/*.tsc` files (TOML manifests).
- Consumer = `scripts/measure-coherence.sh` (reads `REGISTRY="targets/registry.tsc"`, loops over `TARGETS=(repo hypothesis method evidence)` invoking `coh` per target).
- Consumer (mental) = the wave-manifest forward-reference contract for `targets/hypothesis.tsc` (eight paths in pinned order).
- Generated output = `.tsc/`. Excluded from `.gitignore`, from `sources` arrays in target manifests, and from human ambiguity via comment-block preambles in `registry.tsc` and `repo.tsc`.
- No non-primary-language harness drift: there is no shell fixture or CI workflow that produces TSC manifests; the registry is the only producer.

Polyglot re-audit (row 9) applied across all four languages in the diff: TOML (`tomllib.load` on all five files), Bash (`bash -n` + direct run of the missing-`coh` branch), Markdown (cross-references resolve + intra-doc grep counts), gitignore (syntax-consistency with existing entries). Each language audit ran cleanly.

## Cross-cycle pattern observations (cph cycles 5–13 vs cycle 14)

Patterns relative to prior cph cycles, with cycles 12 (Sub A) and 13 (Sub B) as immediate predecessors in the same wave:

- **Third wave-mode dispatch in cph; first MCA-eligible sub in this wave.** Cycles 12 and 13 were both design-and-build (docs-only). Cycle 14 is MCA-eligible — the target-manifest format string, four-target slicing, script shape, and AC-PROJECT field list are all spec-pinned, leaving α's surface as mechanical assembly + grep-level evidence rather than narrative authoring. The wave manifest's §Cycle scope sizing table predicted this profile ("MCA-eligible" + "1–2 review rounds historically"); the actual outcome (1 round, 0 findings) matches the prediction.
- **APPROVE-round-1 streak holds at nine.** Cycles 5, 6, 7, 8, 9, 10, 12, 13, 14 all closed as 1-round APPROVE per available close-outs and this cycle's record. No `cycle/N` in this range carries a fix-round commit chain. The streak now spans both docs-only-cycles and one MCA-eligible infra cycle; the α-side pre-review-gate discipline transfers cleanly from the narrative regime to the structural regime.
- **CI-absence streak at six (cycles 8, 9, 10, 12, 13, 14).** Six consecutive cycles where `.github/workflows/` does not exist and rule 3.10 / α §2.6 row 10 resolves as vacuous. Cycle 14 is the first cycle in the streak with a shell entrypoint where a CI run *could* plausibly exist (lint, `bash -n`, missing-`coh` smoke); none was added. Class: cph is structurally a docs-and-research repo where the CI rule's design assumption does not fire; the wave manifest's "Run `coh` (TSC CLI) in CI: best-effort" line accepts this.
- **Docs-only-cycle dominance breaks at cycle 14.** Cycles 7, 8, 9, 12, 13 were docs-only; cycle 10 was code + docs; cycle 14 is shell + TOML + Markdown + gitignore (infra + docs). Cycle 14 is the second non-pure-docs cycle in the recent run, and the first since cycle 10. The α-side artifact-set differences fire: harness audit (§2.4) applies for the first time this wave; polyglot re-audit (row 9) applies with more than one language for the first time this wave; structural-only test justification (§CDD-Trace step 4) replaces the docs-only "no tests apply" framing.
- **Wave-mode parallel-cycle transient-row suppression — third consecutive cycle.** Cycles 12, 13, 14 all held α §2.6 rows 1 (rebased on main) and 10 (CI green) without drift because siblings ran on `cycle/N` branches and CI does not exist. The drift class α §2.6 closing note flags fired zero times across the wave so far.
- **Implementation-SHA convention — third clean application.** Cycle 12 was the first cph cycle to apply the "implementation SHA, not readiness-signal HEAD" convention; cycles 13 and 14 applied the same convention. Three convergences without a self-stale loop. The #301 F3 derivation transfers cleanly to cph in both docs-only and MCA-eligible modes.
- **No new wave-N → wave-(N+1) protocol-patch candidate surfaced this cycle.** Cycle 12's β surfaced rule 3.11b vs wave-mode γ-artifact-path divergence as a non-binding observation; cycle 13's sub-issue body carried the γ=δ exemption inline (the intra-wave patch tightening α #13 named); cycle 14's β consumed the same exemption signal (sub-issue body's `γ = δ permitted.` line + wave-manifest dispatch comment) and recorded it as a resolved read in `beta-review.md` §2.5. The patch loop tightened from "downstream γ-skill work" (cycle 12) to "discoverable in the sub-issue body" (cycle 13) to "consumed without raising it" (cycle 14) across three consecutive intra-wave cycles. Pattern: the protocol-patch loop α #7 / #8 / #9 named at cross-wave scale and α #13 named at intra-wave scale appears to stabilize after one or two passes rather than persisting as long-lived ambiguity.
- **Known-debt item count rose with the change-shape.** Cycle 12 (Sub A, docs-only) carried no §Debt items beyond declared-narrative-empirical-state preservation; cycle 13 (Sub B, docs-only) carried zero §Debt items in the strict sense (status-call defensibility was self-check material, not debt); cycle 14 (Sub C, MCA-eligible infra) carries five §Debt items (no numeric C_Σ baseline; `coh` flag spelling inferred; per-target schema choice; `shellcheck` not run; gitignore-convention scope note). Pattern: MCA-eligible infra subs surface more known debt at α's gate than docs-only subs of the same wave, because more of the surface depends on an external tool the dispatch environment does not provide. β surfaced none of the five as a finding.

## Wave position

Sub C is the third of four subs to close in `cdr-refactor-2026-05-18`. Sub A (cph#12) merged at `c3c274a`; Sub B (cph#13) merged at `49cd888`; Sub C (cph#14) merged here at `f6ad183`. Sub D (cph#15) is now unblocked under the wave manifest's "A+B+C terminal state" gate and is the wave's conformance-sweep cycle (ACs 8, 9, 10 final across A+B+C). `.cdd/unreleased/14/` remains in place until δ's wave-level disconnect per β's close-out and the operator-as-γ-and-δ pattern; α does not move directories on individual sub merges within a wave, and this close-out lands on `main` directly per the dispatch's standing permissions (no tag, no release — wave-internal).

α's work on Sub C ends here.
