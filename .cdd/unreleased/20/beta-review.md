# β review — Sub D — cph#20 — F4

## Round 1

**Verdict:** APPROVE

**Round:** 1
**Wave:** `.cdd/waves/coherence-drift-sweep-2026-05-18/`
**Master:** usurobor/cph#16
**Sub:** usurobor/cph#20
**Base SHA (Sub D parent):** `319ab3e` (Sub C self-coherence)
**Implementation SHA:** `8859b62`
**Self-coherence SHA:** `00f38b3`
**Branch CI state:** N/A (no `.github/workflows/`)
**Branching:** single-branch dispatch on `claude/review-repo-coherence-PNbjQ` per wave manifest §"Branching deviation"

## Identity-audit

| Commit | Expected author | Observed | Pass |
|---|---|---|---|
| `8859b62` (impl) | `alpha@cph.cdd.cnos` | `α-as-agent <alpha@cph.cdd.cnos>` | ✓ |
| `00f38b3` (self-coherence) | `alpha@cph.cdd.cnos` | `α-as-agent <alpha@cph.cdd.cnos>` | ✓ |
| this review commit | `beta@cph.cdd.cnos` | will be authored as `β-as-agent <beta@cph.cdd.cnos>` | ✓ (pre-commit) |

Identity-isolation invariant (α ≠ β within Sub D) preserved.

## AC-by-AC

### AC1 — F4 closed in live surfaces

**Oracle (dispatcher's prompt, verbatim issue body):** `grep -rln "gait-support-paths-features" .`. β re-ran:

```
$ grep -rln "gait-support-paths-features" . --exclude-dir=.git
reports/field-report-01-existing-data-zeroth-pilot.md
.cdd/unreleased/6/beta-review.md
.cdd/unreleased/20/self-coherence.md
.cdd/unreleased/10/beta-review.md
.cdd/unreleased/10/beta-closeout.md
.cdd/unreleased/10/self-coherence.md
.cdd/waves/coherence-drift-sweep-2026-05-18/status.md
.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md
```

All eight remaining hits are inside the two AC1-allowed surfaces:
- `reports/field-report-01-existing-data-zeroth-pilot.md` (frozen field report — issue body §Scope explicitly excludes editing this file; reference at L270 is preserved as the report's historical cite of where data was written at authoring time).
- `.cdd/**` (historical CDD artifacts: `.cdd/unreleased/{6,10}/*` are prior-cycle close-out artifacts; `.cdd/unreleased/20/self-coherence.md` is α's own self-coherence text *for this sub* which legitimately names the old string in §Gap; `.cdd/waves/.../status.md` and `.cdd/waves/.../manifest.md` are wave-scoped scaffolding that α correctly identified as out-of-scope per dispatcher's prompt "Do NOT edit `.cdd/**` historical artifacts").

Note that β's own `beta-review.md` (this file) will add to the `.cdd/**` count when committed — that addition is also inside the allowed surface.

**Positive oracle (dispatcher's prompt):** `grep -rln "cph-features" .` returns hits in the four live surfaces + the regenerated notebook. β re-ran:

```
$ grep -rln "cph-features" . --exclude-dir=.git
scripts/io_opencap.py
scripts/build_notebook.py
notebooks/README.md
notebooks/existing-data-processing.ipynb
analysis/feature-summary-zeroth-pilot.md
.cdd/unreleased/20/self-coherence.md
.cdd/waves/coherence-drift-sweep-2026-05-18/manifest.md
```

Five live surfaces (four source + one generated notebook) plus two `.cdd/**` non-edit mentions (α's self-coherence and the wave-manifest's pre-existing recommendation at line 39). The four source live surfaces — `scripts/io_opencap.py`, `scripts/build_notebook.py`, `notebooks/README.md`, `analysis/feature-summary-zeroth-pilot.md` — match the issue body §Scope's named four-file list exactly.

**Per-file count check (dispatcher's prompt sub-oracle):**

```
$ grep -c "cph-features" scripts/io_opencap.py scripts/build_notebook.py notebooks/README.md analysis/feature-summary-zeroth-pilot.md notebooks/existing-data-processing.ipynb
scripts/io_opencap.py:1
scripts/build_notebook.py:2
notebooks/README.md:1
analysis/feature-summary-zeroth-pilot.md:1
notebooks/existing-data-processing.ipynb:2
```

Counts:
- `scripts/io_opencap.py`: 1 (comment at L31) — matches expected.
- `scripts/build_notebook.py`: 2 (L307 generated code cell, L382 generated markdown cell) — matches expected.
- `notebooks/README.md`: 1 (override-docs line) — matches expected.
- `analysis/feature-summary-zeroth-pilot.md`: 1 (provenance bullet) — matches expected.
- `notebooks/existing-data-processing.ipynb`: 2 (generated by `build_notebook.py` from L307 + L382 source cells) — matches expected.

**Rename consistency spot-check.** β read each rename site:

| File | Pre-patch | Post-patch | Surrounding-character preserved? |
|---|---|---|---|
| `scripts/io_opencap.py:31` | `gait-support-paths-features/` (with trailing slash in path) | `cph-features/` (trailing slash) | ✓ |
| `scripts/build_notebook.py:307` | `"gait-support-paths-features"` (no slash; Python string, slash added by `DATA_ROOT / "..."`) | `"cph-features"` (no slash) | ✓ |
| `scripts/build_notebook.py:382` | `gait-support-paths-features/features-zeroth-pilot.csv` (slash before filename) | `cph-features/features-zeroth-pilot.csv` (slash before filename) | ✓ |
| `notebooks/README.md:35` | `<GAIT_DATA_ROOT>/gait-support-paths-features/` (slash before + after) | `<GAIT_DATA_ROOT>/cph-features/` (slash before + after) | ✓ |
| `analysis/feature-summary-zeroth-pilot.md:27` | `$GAIT_DATA_ROOT/gait-support-paths-features/features-zeroth-pilot.csv` | `$GAIT_DATA_ROOT/cph-features/features-zeroth-pilot.csv` | ✓ |

No typo'd variants (no `cph_features` underscore, no `cph-feature` singular). Slash discipline preserved per pre-patch convention.

**Verdict:** AC1 met.

### AC2 — Notebook regenerated

**Oracle (issue body §Proof plan "Smoke"):** `python3 scripts/build_notebook.py` exits 0 *and* the regenerated notebook contains the new name (verified by grep against the .ipynb JSON).

β does not re-run `build_notebook.py` in this review session (α already ran it; the regenerated notebook is committed at `8859b62`). β verifies via grep + structural inspection:

```
$ grep -c "cph-features" notebooks/existing-data-processing.ipynb
2
$ grep -c "gait-support-paths-features" notebooks/existing-data-processing.ipynb
0
```

The regenerated notebook carries exactly 2 references to the new name (one code cell, one markdown cell — matching the two `build_notebook.py` source cells at L307 and L382). Zero references to the old name remain.

**Structural spot-check (dispatcher's prompt: "spot-check of the rename in the generated source"):** β parsed the .ipynb JSON via `python3 -c "import json; nb=json.load(open(...))"` and enumerated the 21 cells:

| Cell | Type | First line |
|---|---|---|
| 0 | md | `# Existing-data zeroth pilot — pipeline` |
| 1 | code | `# Configuration` |
| 2 | md | `## 1. Discover trials` |
| 3 | code | `from scripts.io_opencap import (` |
| 4 | md | `## 2. Gait-cycle segmentation (AC1)` |
| 5 | code | `from scripts.segmentation import segment_trial, summary_table` |
| 6 | code | `# AC1 oracle: ≥80% of walking trials yielded ≥1 cycle.` |
| 7 | md | `## 3. Feature table (AC2)` |
| 8 | code | `from scripts.features import build_feature_table, missingness` |
| 9 | code | `miss = missingness(features)` |
| 10 | code | `# AC2 oracle: missingness < 20% across feature columns` |
| 11 | md | `## 4. First-pass plots (AC3)` |
| 12 | code | `from scripts.segmentation import time_normalize_cycle` |
| 13 | code | `# Condition comparison: walking vs walkingTS` |
| 14 | code | `# Feature distribution: knee range across cycles` |
| 15 | md | `## 5. OpenCap-vs-reference comparison (AC4)` |
| 16 | code | `from scripts.comparison import compare_joints` |
| 17 | code | `# AC4 oracle: per-source mean Pearson r ≥ 0.7 across knee+hip+ankle.` |
| 18 | md | `## 6. Persist feature table (private — not committed)` |
| 19 | code | `PRIVATE_OUT = DATA_ROOT / "cph-features"` |
| 20 | md | `## 7. Notebook self-check` |

Cell 19 (the persistence cell) carries the rename directly: `PRIVATE_OUT = DATA_ROOT / "cph-features"`. Cell 20 (§7 "Known debt") is preserved — important for Sub C's cross-reference at `scripts/build_notebook.py:400` to still resolve (β verified `grep -nE 'Known debt' scripts/build_notebook.py` returns L400; Sub D's edits to `build_notebook.py` were at L307 and L382 only, leaving L400 intact).

Section sequence (§1–§7) is intact. No cell loss. The 1418-line diff against the pre-patch notebook is the cached-execution-output strip α correctly disclosed in §Debt 2 — `build_notebook.py` emits cells without `outputs[]` arrays; the prior committed notebook had cached outputs from a `jupyter nbconvert --execute` run that are dropped on regeneration. Substantive change is the two rename references; the rest is incidental cached-output removal. Issue body AC2 does not require re-execution (`jupyter nbconvert --execute` needs the OpenCap Lab Validation archive, which is operator-only per wave manifest §"Standing permissions" implicit constraint).

**`pip install nbformat==5.10.4` audit (α flag 4):**

Wave manifest §"Standing permissions" reads `Install Python packages: NO (this wave does not touch \`requirements.txt\`)`. β reads the parenthetical as the *rationale* for the prohibition: prevent dependency drift / pin changes mid-wave. The literal install (`pip3 install nbformat==5.10.4`) matches the pre-existing pin at `requirements.txt:16` verbatim — no pin moved, no `requirements.txt` modification:

```
$ git diff 319ab3e..8859b62 -- requirements.txt
(empty)
```

β reads this as install-to-match-spec, not a dependency change. The wave manifest's intent (no dependency drift) is honored; the requirements.txt pin is unchanged; AC2's regeneration mandate is satisfied without violating the underlying invariant. β does *not* flag as a policy breach. The dispatcher prompt named this case explicitly and recommended the same disposition.

A strict-literal reading would have made AC2 infeasible (without `nbformat`, `build_notebook.py` cannot run — the regeneration mandate is infeasible). The strict reading is therefore self-defeating for the wave. β surfaces this to ε in §Cross-sub debt for explicit operator confirmation, per dispatcher's prompt.

**Verdict:** AC2 met.

### AC3 — No empirical drift

**File-surface check:**

```
$ git diff --name-only 8859b62^..8859b62
analysis/feature-summary-zeroth-pilot.md
notebooks/README.md
notebooks/existing-data-processing.ipynb
scripts/build_notebook.py
scripts/io_opencap.py
```

Five files, all expected per wave manifest §"Issues" for Sub D and per issue body §Scope.

**Charter-doc / ledger / report check:**

```
$ git show 8859b62 -- README.md PROJECT.md ROADMAP.md CHANGELOG.md
(empty)
```

No charter docs touched.

```
$ git diff --diff-filter=A --name-only 8859b62^..8859b62
(empty)
```

No new files added (no new field report).

**REVISE posture check:** β re-read PROJECT.md L20 §"Current empirical decision" at HEAD: still `**REVISE** (2026-05-17 real-data run, per [reports/field-report-01-existing-data-zeroth-pilot.md]...)`. Unchanged.

**Substantive content check (analytic logic):** β read the diff to `scripts/build_notebook.py`:

```
$ git diff 8859b62^..8859b62 -- scripts/build_notebook.py
```

Two line changes: L307 (`PRIVATE_OUT = DATA_ROOT / "cph-features"` from `"gait-support-paths-features"`) and L382 (provenance markdown string). Both are string-literal substitutions inside cell source bodies — generator input, not generator logic. No analytic cell content changed.

`scripts/io_opencap.py` change is a comment-only edit at L31 (β verified by reading the diff: the change is inside a `#` line, no executable surface).

**Verdict:** AC3 met.

### AC4 — Historical references intact

**`reports/field-report-01-existing-data-zeroth-pilot.md` unchanged:**

```
$ git diff 8859b62^..8859b62 -- reports/
(empty)
```

Empty. The L270 reference to `gait-support-paths-features/` survives verbatim (β confirmed via `grep -n "gait-support-paths-features" reports/field-report-01-existing-data-zeroth-pilot.md` returns line 270 unchanged).

**`.cdd/**` historical artifacts unchanged:**

```
$ git diff 8859b62^..8859b62 -- '.cdd/'
(empty)
```

Empty. Prior-cycle artifacts in `.cdd/unreleased/{6,10}/` are not modified. Wave-scoped scaffolding (`.cdd/waves/coherence-drift-sweep-2026-05-18/{status.md,manifest.md}`) is not modified — including the manifest's own naming-recommendation mention at line 39, which α correctly identified as out-of-edit-scope per dispatcher's prompt.

**Verdict:** AC4 met.

## Notes

**N1 (α flag 4 — `pip install nbformat==5.10.4` policy interpretation).** Dispatcher's prompt invited β to make this call independently. β-side disposition: install-to-match-spec (pin already in `requirements.txt:16`; no `requirements.txt` modification; no dependency drift). Wave manifest's prohibition rationale is honored. Not flagged as a policy breach. Named in §Cross-sub debt for ε review.

**N2 (notebook diff size, α §Debt 2).** The `notebooks/existing-data-processing.ipynb` diff is ~1418 deletions / ~70 insertions because the pre-patch notebook carried cached execution outputs (`jupyter nbconvert --execute` outputs including stderr lines, plot binaries, and DataFrame.to_html() renderings) that `build_notebook.py` does not emit (the builder emits cells without `outputs[]` arrays). The substantive rename is 2 character-strings in the cell sources; the remaining ~1300 lines is cached-output strip. β read the diff with this framing — the substantive change is the two rename references in cell 19 (code) and cell 20-adjacent (markdown summary, in section §6 inside the persistence flow). Sub C's `analysis/features.md` cross-reference to "notebook §7 Known debt" still resolves: §7 cell (markdown) is preserved as cell 20 in the regenerated notebook.

**N3 (notebook structural integrity).** β parsed the .ipynb JSON and enumerated all 21 cells (table in §AC2 above). All 7 sections (§1–§7 plus title and Configuration) are present. Cell sequence and types match the expected `build_notebook.py` emission. No analytic loss.

**N4 (`scripts/io_opencap.py` change is comment-only).** L31 is a `#`-prefixed line. The change has zero executable surface effect. Verified by reading the diff.

**N5 (wave manifest mention at line 39).** The wave manifest names both `cph-features/` and `gait-cycle-features/` as the two operator-recommendation candidates Sub D's α can pick from. After α picks `cph-features/`, that mention becomes slightly redundant (the recommendation has been concretely resolved). Editing the manifest would mean editing wave scaffolding, which is out of Sub D scope per dispatcher's prompt. α correctly identified this in §Debt 3.

**N6 (cross-sub consistency).** Sub C's `analysis/features.md` cross-references `scripts/build_notebook.py:400` (the §7 "Known debt" cell generator). Sub D's edits to `scripts/build_notebook.py` were at L307 and L382 only. β verified `grep -nE 'Known debt' scripts/build_notebook.py` still returns L400 unchanged. The cross-sub citation remains valid.

## Scope-drift check

| Surface | Expected per wave manifest §"Issues" + cph#20 §Scope | Touched in diff? |
|---|---|---|
| `scripts/io_opencap.py` (comment at L31) | yes | yes (L31, comment-only) |
| `scripts/build_notebook.py` (L307 generated code, L382 generated markdown) | yes | yes (L307, L382 — both inside `new_*_cell(...)` arguments) |
| `notebooks/README.md` (override docs L35) | yes | yes (L35) |
| `analysis/feature-summary-zeroth-pilot.md` (provenance L27) | yes | yes (L27) |
| `notebooks/existing-data-processing.ipynb` (regenerated artifact) | yes (issue body §Scope: "Regenerate the notebook so its embedded references match") | yes (regenerated by `python3 scripts/build_notebook.py`) |
| `reports/field-report-01-existing-data-zeroth-pilot.md` | no (issue body §Non-goals + wave manifest §"Known constraints") | no |
| `.cdd/**` historical artifacts | no (dispatcher's prompt) | no |
| `requirements.txt` | no (wave manifest §"Standing permissions") | no |
| `GAIT_DATA_ROOT` env var | no (issue body §Non-goals: "Renaming `GAIT_DATA_ROOT` or any other env var") | no |
| README/ROADMAP/PROJECT/CHANGELOG | no | no |
| Other code modules | no | no |

No scope-drift. α stayed inside Sub D's surface exactly. The five files touched match the wave manifest §"Issues" file list for Sub D verbatim.

## Cross-sub debt (for δ wave-closeout)

1. **`pip install nbformat==5.10.4` policy interpretation.** β-side disposition: install-to-match-spec is not a policy breach. ε may wish to revisit the wave manifest §"Standing permissions" wording to either (a) explicitly allow install-to-match-spec, or (b) explicitly prohibit it (which would make notebook regeneration infeasible in environments where `nbformat` is not pre-installed, requiring a wave-manifest-level resolution). β surfaces for ε review per dispatcher's prompt; β does not block on this.

2. **Operator-side on-disk migration `gait-support-paths-features/` → `cph-features/`.** Wave manifest §"Out-of-scope follow-ups" second bullet names this. Operators currently using `<GAIT_DATA_ROOT>/gait-support-paths-features/` will see the notebook create a parallel `cph-features/` directory on first re-run. The pre-existing data persists in the old directory; the operator owns the migration. α §Debt 4; β concurs.

3. **Notebook re-execution to re-bake cell outputs.** Sub D's regeneration via `build_notebook.py` produces a clean notebook with no cached outputs. Re-baking the outputs requires `jupyter nbconvert --execute --inplace notebooks/existing-data-processing.ipynb` against the OpenCap Lab Validation archive (operator-only data). Operators with the archive can re-execute locally; the in-repo notebook stays clean. α §Debt 2; β concurs.

4. **Wave manifest line 39 naming-recommendation mention.** Now slightly redundant (the recommendation has been resolved to `cph-features/`). Editing wave scaffolding is out of Sub D scope. ε may wish to update the manifest as part of the wave-closeout if convention prefers it. α §Debt 3.

## Round 1 close

AC1 / AC2 / AC3 / AC4 all met under independent β re-run. The `pip install nbformat==5.10.4` exception is not flagged as a policy breach (install-to-match-spec; pin pre-existed; no dependency drift; surfaced to ε in §Cross-sub debt). The notebook diff size is explained by cached-output strip, not substantive change. No scope-drift. No identity-isolation breach. Sub D APPROVE.
