# Roadmap

This file tracks the gates by which the Coherence Path Hypothesis is validated, revised, or abandoned.

The hypothesis itself lives in [docs/concepts/coherence-path-hypothesis.md](docs/concepts/coherence-path-hypothesis.md). Live operational status lives in [PROJECT.md](PROJECT.md). The project changelog lives in [CHANGELOG.md](CHANGELOG.md). This roadmap names what each phase must achieve and where each phase stands.

## Goal

Validate, revise, or abandon the Coherence Path Hypothesis through gates that name what each phase must produce, what currently exists, and what blocks the next transition.

## Current state

**R1 is GO with bounded scope (path (a) inferred bilateral); R3 R-side is partial GO; R3 bilateral is indeterminate on inferred-bilateral surface (path (a)); R4 is construct survives subject to anchor caveats** per [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md) (2026-05-19 cph#28 L-cycle recovery), [`reports/field-report-03-construct-evaluation.md`](reports/field-report-03-construct-evaluation.md) (2026-05-19 cph#27 R3 R-side first-pass + 2026-05-20 cph#30 R3 bilateral extension), and [`reports/field-report-04-falsification-evaluation.md`](reports/field-report-04-falsification-evaluation.md) (2026-05-20 cph#31 R4 full falsification re-evaluation). The OpenCap-vs-reference comparison cleanly passes (R1); the gait-cycle segmentation primitive passes AC1 on R-side (R2 GO at the detector level) and on L-side via contralateral inference (R1 transition gate satisfied via cph#28: 57 L cycles, all inferred-partial, mean coverage 0.86, calibrated against the lone measured L cycle to 30 ms); the R-side construct evaluation produced 7 BH-significant condition responses (q<0.05) across 25 numeric features in 10 subjects, with three candidate support-path hypotheses surfaced. cph#30's R3 bilateral extension ran the L-vs-R lr-diff aggregate on the post-cph#28 inferred-bilateral surface (60 R + 57 L; 57 paired rows; 25 lr_diff features × 2 conditions = 50 paired tests; 21 BH-significant at q<0.05, all positive direction R > L) and returns **indeterminate** on H3 and on falsification condition 3 — the positive-direction lr-diff pattern is consistent with both a real asymmetric coordination signature and the partial-clip geometric bias predicted by cph#28's matched-duration L-cycle recovery; the two surfaces cannot be distinguished without measured-bilateral data. cph#31's R4 walks all 6 falsification conditions one by one and reports 0 of 6 triggered, 5 of 6 cleanly NOT triggered (conditions 1, 2, 4, 5, 6 anchored on cph#27 R-side aggregate + R1 OpenCap-vs-reference + 0.00% missingness on 117 cycles); condition 3 was deferred to cph#30 at cph#31 run time and is updated by this cycle to "evaluable; indeterminate; non-contributory to the threshold tally" — the wave-level verdict reading (0 of 6 triggered → construct survives subject to caveats) is preserved unchanged, with one strengthened caveat (path (b) measured-bilateral upgrade is now identified as the route to a decisive H3 / condition 3 verdict). R5 is unblocked at the gate level (R3 closes on both surfaces; R4 survives) but the capture-protocol-revision (≥3 s trial length) is the remaining R5 prerequisite. R6 is not started; R3-bilateral indeterminate does not unblock R6.

## How to read this file

Each phase carries seven fields:

- **Goal** — what the phase must achieve.
- **Current evidence** — what merged artifacts show today; cited from owning files.
- **Gate** — the condition that closes the phase. "Closes" means status transitions out of ACTIVE / REVISE into GO.
- **Status** — one of NOT STARTED, ACTIVE, GO, REVISE, STOP. R5 and R6 carry their own status phrasings per the master issue.
- **Coherence risk** — how running this phase could damage project coherence (terminology drift, evidence/claim drift, identity drift). Not an empirical risk; the empirical risks live in the protocol and field-report files.
- **Next action** — the next concrete step the phase requires.
- **Owning files** — the surfaces that this phase reads from and writes to.

Phase transitions are gate-based. Each phase moves only when its specific gate is satisfied.

## Phase R0 — Charter coherence

- **Goal:** Make the Coherence Path Hypothesis explicit, bound its operational terms, and pin source-of-truth ownership across the repo.
- **Current evidence:** [`README.md`](README.md), [`docs/concepts/coherence-path-hypothesis.md`](docs/concepts/coherence-path-hypothesis.md), [`docs/concepts/support-path.md`](docs/concepts/support-path.md), [`docs/concepts/failure-conditions.md`](docs/concepts/failure-conditions.md), and [`docs/articles/seven-ways-people-walk.md`](docs/articles/seven-ways-people-walk.md) describe one project. README repositions cph as the gait-domain empirical case for the broader coherence-object theory in [`usurobor/cog`](https://github.com/usurobor/cog).
- **Gate:** README, hypothesis doc, support-path doc, failure-conditions doc, ROADMAP, and PROJECT.md agree on terms, empirical state, and source-of-truth ownership.
- **Status:** GO (closed).
- **Coherence risk:** Charter files re-state the hypothesis or its operational terms with subtly different wording. The risk is that `coherence path`, `support path`, `gait family`, and `feature` lose their boundaries across files. Drift-sweep waves (master issues against the review branch) are the structural backstop.
- **Next action:** None for R0 itself. Future drift sweeps run as needed when charter surfaces are edited.
- **Owning files:** [`README.md`](README.md), [`docs/concepts/coherence-path-hypothesis.md`](docs/concepts/coherence-path-hypothesis.md), [`docs/concepts/support-path.md`](docs/concepts/support-path.md), [`docs/concepts/failure-conditions.md`](docs/concepts/failure-conditions.md), [`docs/articles/seven-ways-people-walk.md`](docs/articles/seven-ways-people-walk.md), `ROADMAP.md` (this), [`PROJECT.md`](PROJECT.md), [`CHANGELOG.md`](CHANGELOG.md).

## Phase R1 — Existing-data zeroth pilot

- **Goal:** Process the OpenCap Lab Validation archive end-to-end before any new data collection, and test whether the pipeline can produce usable gait-cycle segmentation, feature tables, plots, and reference comparisons on real walking data.
- **Current evidence:** Per [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md) (2026-05-19 cph#28 L-cycle recovery run), the archive (60 walking trials × 10 subjects) was acquired (SHA-256 `3290d485124fd12c85dd3bc9ee851f3a0530ad0ff58bc396973e665dd6d28187`), extracted, and run end-to-end. Discovery, IK parsing, marker pairing, feature extraction, OpenCap-vs-reference comparison, plotting, and persistence are real-data-ready. The OpenCap-vs-reference comparison passes (Pearson r̄ 0.962 HRNet, 0.933 OpenPose_default, 0.951 OpenPose_highAccuracy across 60 trials × 3 Video backbones). The segmentation primitive passes AC1 on R-side (60/60 trials, 100%; 60 R cycles, mean 1.06 s, R-only range 0.89–1.37 s) and on L-side via cph#28's contralateral-anchored inference (57/60 trials, 95%; 57 L cycles, all inferred-partial, coverage 0.80–0.94, mean coverage 0.86). Bilateral (subject, trial_id, cycle_number) pair count is 57; `scripts.features.lr_asymmetry` computes on 60 non-empty rows. Feature-extraction missingness is 0.00% across 35 columns × 117 cycles.
- **Gate:** Pipeline produces usable gait-cycle segmentation, feature tables, plots, and reference comparisons on the existing archive. All four artifact classes must clear their AC thresholds.
- **Status:** **GO with bounded scope (path (a) inferred bilateral).** The cph#28 AC5 GO criterion fires on both surfaces (AC1 ≥ 80% on R 100% and L 95%; L ≥ 10 with 57 cycles). The bounded scope is operational: all 57 L cycles are inferred via the half-stride contralateral model and partial-clip (no full-coverage L cycles exist on this archive due to trial cropping), so bilateral asymmetry features carry an inference layer that R-vs-R features do not. Path (b) (operator-side OpenSim IK rerun on the reachable TRC files) remains the path to truly measured bilateral data and is the upgrade option if the inferred-bilateral analyses surface ambiguities that need measurement to resolve.
- **Coherence risk:** Mainline files cite branch-only segmentation numbers as fact, or treat the OpenCap-vs-reference pass as evidence for the Coherence Path Hypothesis itself. The comparison cleared the OpenCap *technology stack*, not the construct. A new risk surfaced by cph#28's GO transition: reading the path (a) inferred-bilateral surface as a *measured* bilateral surface. The field report explicitly bounds L-side claims as "consistent with an asymmetric coordination signature" rather than as direct measurement; downstream R3 / R4 reports must carry the same caveat. *Coherence laundering* (treating inferred features as measurement-grade) is the failure mode to avoid in any bilateral analysis run on the post-cph#28 feature table.
- **Next action:** R1 closes GO with bounded scope. The R3 bilateral extension (re-run `analysis/r3_subject_aggregate_tests.py` against the post-cph#28 feature table to include L-side rows + R-vs-L subject-paired deltas) and the R4 full falsification re-evaluation (condition 3 now evaluable on the inferred-bilateral surface) are unblocked.
- **Owning files:** [`protocols/existing-data-zeroth-pilot.md`](protocols/existing-data-zeroth-pilot.md), [`notebooks/existing-data-processing.ipynb`](notebooks/existing-data-processing.ipynb), `scripts/io_opencap.py`, `scripts/build_notebook.py`, `scripts/segmentation_contralateral.py`, [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md), [`data/external/opencap-lab-validation.md`](data/external/opencap-lab-validation.md).

## Phase R2 — Contact-event and segmentation reliability

- **Goal:** Establish reliable heel-strike detection and gait-cycle segmentation on real Mocap calcaneus markers, on both sides or with a clearly justified one-side analysis path.
- **Current evidence:** Per [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md) §Segmentation Status (post-fix), `scripts.segmentation.detect_heel_strikes` now uses robust-percentile normalization plus stance-region depth/length gating: each contiguous run of `yn < 0.30` (where `yn = (smoothed_heel − q05) / (q95 − q05)`) lasting ≥150 ms AND reaching `< 0.10` is one stance phase, with HS at the first sample inside that deep-stance plateau. The detector is invariant to absolute height, baseline offset, and amplitude. Result: 60/60 R-side trials produce ≥1 cycle (100%; 30/30 natural + 30/30 trunk-sway); L-side fires HS on all 60 trials but only 1 trial yields a complete L cycle, with 47/60 ending in mid-swing and 12/60 starting in mid-swing — a property of the source archive's trial cropping (~1.3–1.5 s, R-stride-aligned). The smoke synthetic regenerates 7+7 = 14 cycles at the expected 1.1 s stride period. The bounded revision shipped on [`cycle/port-segmentation-fix`](https://github.com/usurobor/cph/tree/cycle/port-segmentation-fix) (cph#26 step A); the original orthogonal branch `origin/cycle/segmentation-real-data-fix` (tip `a95415c`) was its precursor and is superseded.
- **Gate:** Detector fires on ≥60% of real Mocap walking trials per side, *or* the report explicitly justifies a one-side analysis path with documented reasoning for the asymmetry.
- **Status:** GO. The detector clears the 60% threshold cleanly on the R-side, and the one-side analysis path is justified in [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md) by per-(trial, side) classification (`scripts/segmentation_diagnostics.py`) showing the L-side mid-swing endings are a trial-cropping property of the source archive, not a detector failure. L-side cycle recovery (contralateral-anchored detection or wider-window IK) is a downstream R3 / friend-pre-pilot-capture concern, not an R2 detector concern.
- **Coherence risk:** Reading R2 GO as removing the R1 REVISE constraint. R2 closes the detector subproblem; R1 stays REVISE because the bilateral construct still depends on L-cycle yield, which is a data-shape constraint that R2 cannot address. A second risk: treating smoke-test passing as construct evidence; the smoke generator hardcodes the heel shape the detector targets, which is a tautology, not validation.
- **Next action:** Closed. Future work on L-cycle recovery is named in R1's Next action and in [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md) §Decision.
- **Owning files:** `scripts/segmentation.py`, `scripts/segmentation_diagnostics.py`, [`notebooks/existing-data-processing.ipynb`](notebooks/existing-data-processing.ipynb), [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md).

## Phase R3 — First construct-level evidence

- **Goal:** Test whether the per-cycle features named in [`analysis/features.md`](analysis/features.md) show stable, interpretable coordination signatures at the subject level under condition response (natural vs trunk-sway), without pseudoreplication.
- **Current evidence:** Per [`reports/field-report-03-construct-evaluation.md`](reports/field-report-03-construct-evaluation.md), two passes have run. (1) cph#27 R-side first-pass (2026-05-19): aggregate analysis of 10 subjects × 2 conditions × 25 numeric features, paired Wilcoxon signed-rank on per-subject medians, BH-FDR at q=0.05; 7 BH-significant condition responses including a textbook-magnitude lumbar bending increase (+18.3°, r_rb = +1.0, all 10 subjects in agreement) and a distal sagittal contraction (ankle range −6.6°, r_rb = −1.0); H1 survives in *distal-contraction-under-proximal-compensation* form (hip-as-sagittal-driver sub-claim does not), H2 survives in *trunk-segment-driven* form (hip-adduction-as-compensation sub-claim does not), H3 structurally non-testable on R-side-only surface. Three candidate support-path hypotheses surfaced: Lateral-trunk substitution path, Distal sagittal contraction under proximal compensation, Cadence-slowdown signature. Reproducible via [`analysis/r3_subject_aggregate_tests.py`](analysis/r3_subject_aggregate_tests.py). (2) cph#30 bilateral extension (2026-05-20): L-vs-R lr-diff aggregate on the post-cph#28 inferred-bilateral surface (60 R + 57 L; 57 paired rows; 25 lr_diff features × 2 conditions = 50 paired tests; 21 BH-significant at q<0.05, all positive direction R > L); the cleanest H3 probe (coordination-lag lr_diff features) returns BH-non-sig null; H3 verdict indeterminate (positive-direction range-feature lr-diff *consistent with* both real asymmetric coordination and partial-clip geometric bias predicted by cph#28); H1 / H2 cph#27 verdicts preserved unchanged. Reproducible via [`analysis/r3_bilateral_tests.py`](analysis/r3_bilateral_tests.py).
- **Gate:** Subject-level (not cycle-level) condition-response analysis demonstrates stable feature distributions per subject per condition; aggregation method excludes pseudoreplication; at least two of the three framed hypotheses survive specification under adequate n.
- **Status:** **Partial GO on R-side (cph#27); indeterminate on bilateral inferred-bilateral surface (cph#30).** R-side surface satisfies the gate's three sub-conditions: aggregation is per-subject (pseudoreplication avoided); feature distributions are stable across subjects within each condition (r_rb = ±1.0 on four BH-significant features); two of three framed hypotheses (H1, H2) survive specification in their R-side-evaluable forms. Bilateral surface returns indeterminate on H3 (path (a) honesty caveat: partial-clip geometric bias indistinguishable from real asymmetric coordination signature on inferred-bilateral data; coordination-lag probes return null). The R3 gate is *partially* closed: R-side satisfied (H1 + H2 of the three framed); bilateral indeterminate (H3 of the three framed). A path (b) measured-bilateral upgrade is the route to a decisive H3 verdict and the third hypothesis evaluation.
- **Coherence risk:** Pseudoreplication — reporting per-cycle distributions as if cycles were independent observations. Adjacent risks: `support path` and `coherence path` losing their distinction once recurrence/distinguishability tests run; alternative explanations (trial crop, marker artifact, camera setup, subject morphology, condition labels alone) being collapsed into the "construct survives" verdict instead of being named separately. A risk surfaced by cph#27's partial GO: reading R-side partial-GO as bilateral-construct evidence. A risk surfaced by cph#28: treating the inferred L cycles as measurement-grade. A risk surfaced by cph#30: reading the BH-significant range-feature lr-diff pattern as a positive H3 verdict (it is *consistent with* the partial-clip geometric bias and the inference layer cannot be separated from real asymmetric coordination on this surface). The field reports explicitly bound these claims with "consistent with" not "measurement of" language; the inference layer is named in every paragraph stating a lr-diff result.
- **Next action:** R3 R-side stands closed at partial GO. R3 bilateral stands closed at indeterminate on inferred-bilateral surface. The decisive next step is the **path (b) measured-bilateral upgrade** (operator-side OpenSim IK rerun on the reachable TRC files at `/opt/gait-data/opencap-lab-validation/extracted/` with extended trial windows) — out of cph#30 scope; requires operator-side OpenSim install. When measured-bilateral data exists, re-run `analysis/r3_bilateral_tests.py` against the new feature table to substitute the indeterminate H3 verdict for a decisive one; cph#31 R4 condition 3 row re-reads predictably at that point per cph#31 §"Open issues from cph#31."
- **Owning files:** [`analysis/features.md`](analysis/features.md), [`analysis/feature-table-schema.md`](analysis/feature-table-schema.md), [`analysis/feature-summary-zeroth-pilot.md`](analysis/feature-summary-zeroth-pilot.md), [`analysis/left-right-comparison.md`](analysis/left-right-comparison.md), [`analysis/r3_subject_aggregate_tests.py`](analysis/r3_subject_aggregate_tests.py), [`analysis/r3_bilateral_tests.py`](analysis/r3_bilateral_tests.py), [`notebooks/existing-data-processing.ipynb`](notebooks/existing-data-processing.ipynb), [`reports/field-report-03-construct-evaluation.md`](reports/field-report-03-construct-evaluation.md).

## Phase R4 — Support/coherence-path inference

- **Goal:** Determine whether the coordination signatures from R3 justify the inference to a support/coherence path, distinct from measured data alone.
- **Current evidence:** Per [`reports/field-report-04-falsification-evaluation.md`](reports/field-report-04-falsification-evaluation.md) (2026-05-20 cph#31 R4 full falsification re-evaluation) updated by [`reports/field-report-03-construct-evaluation.md`](reports/field-report-03-construct-evaluation.md) §"R3 bilateral extension (cph#30, 2026-05-20)", the 6-condition table in [`docs/concepts/support-path.md`](docs/concepts/support-path.md) §Falsification is walked condition-by-condition with explicit testability declaration + verdict + bounded scope per row. Tally after cph#30: 0 of 6 triggered; 5 of 6 cleanly NOT triggered (conditions 1, 2, 4, 5, 6 anchored on cph#27 R-side aggregate + R1 OpenCap-vs-reference + 0.00% missingness on 117 cycles); 1 of 6 (condition 3, L/R asymmetry) **evaluable; verdict indeterminate; non-contributory to the threshold tally** (cph#30 substantive verdict: positive-direction lr-diff on every range feature *consistent with* both real asymmetric coordination and the partial-clip geometric bias predicted by cph#28; coordination-lag probes return null; the two surfaces are indistinguishable on the inferred-bilateral data alone — path (b) measured-bilateral upgrade is the route to a decisive verdict). Wave-level verdict: construct survives the falsification gate (0–1 triggered bucket), subject to four anchor caveats — R-side scope for conditions 1/2/6 (cph#30's bilateral readings on range features are also *consistent with* either real asymmetric coordination or geometric artifact, so they do not flip R-side verdicts); condition 3 indeterminate (path (b) measured-bilateral upgrade owns the decisive verdict); path (a) honesty on bilateral surface (inference not measurement; partial-clip coverage 0.80–0.94); "survives R4" ≠ "construct validated."
- **Gate:** The falsification table from [`docs/concepts/support-path.md`](docs/concepts/support-path.md) §Falsification is evaluated on data capable of triggering each condition; the evaluation distinguishes measured surfaces (kinematics, timing, segment coupling, asymmetry, condition response) from inferred interpretation (coherence-path organization, load-transfer reading); alternative explanations are named per [`docs/concepts/failure-conditions.md`](docs/concepts/failure-conditions.md).
- **Status:** **Construct survives R4 contact subject to anchor caveats** (cph#31, refined by cph#30). Threshold tally: 0 of 6 triggered; ≪ the 4-condition fundamental-revision threshold; clean within the "0–1 triggered → construct survives" bucket. cph#30's R3 bilateral extension closed the condition 3 row at "evaluable; indeterminate; non-contributory to the threshold tally" (same threshold treatment as "not testable" per the `support-path.md` §Falsification "Empirical-data prerequisite" discipline); the wave-level verdict is preserved unchanged. The decisive condition 3 verdict awaits a path (b) measured-bilateral upgrade.
- **Coherence risk:** Treating coherence-path inference as a measurement. The terminology distinction (Coherence Path Hypothesis = the claim; support path = the operationalized pattern) collapses if a phase-4 write-up calls a measured feature a "coherence path" without inference, or treats the seven gait families as proven by a clustering output rather than as candidate surface expressions. *Coherence laundering* — treating a clean falsification table or a self-consistent repo as substantive evidence for the hypothesis — is the failure mode to avoid. A risk surfaced by cph#27: reading R-side "0 of 6 triggered" as bilateral-construct survival evidence. A risk surfaced by cph#28: treating the inferred-bilateral surface as a measured-bilateral surface. A risk surfaced by cph#31: reading the wave-level "survives" verdict as construct validation — the survives verdict is a *non-refutation gate*, not a validation. The field report frames each row's verdict by its evaluation surface (R-side aggregate vs inferred-bilateral surface availability vs R1 OpenCap-vs-reference); future cycles must re-read the same way.
- **Next action:** Closed at the synthesis level (cph#31). cph#30 returned "evaluable; indeterminate" on condition 3 — the threshold tally is unchanged at 0 of 6 triggered (indeterminate is non-contributory per the "Empirical-data prerequisite" discipline). The decisive condition 3 verdict awaits a path (b) measured-bilateral upgrade (operator-side OpenSim IK rerun on the reachable TRC files at `/opt/gait-data/opencap-lab-validation/extracted/` with extended trial windows). The condition 3 row in field-report-04 reads from cph#30's verdict via the cph#30 field report append section; future path (b) closure can update both reports.
- **Owning files:** [`docs/concepts/support-path.md`](docs/concepts/support-path.md) (§Falsification — the canonical conditions and threshold rule), [`docs/concepts/failure-conditions.md`](docs/concepts/failure-conditions.md), [`docs/concepts/coherence-path-hypothesis.md`](docs/concepts/coherence-path-hypothesis.md) (§Falsification conditions, §Current empirical status), [`reports/field-report-03-construct-evaluation.md`](reports/field-report-03-construct-evaluation.md) (R-side aggregate that anchors conditions 1/2/6), [`reports/field-report-01-existing-data-zeroth-pilot.md`](reports/field-report-01-existing-data-zeroth-pilot.md) (R1 / cph#28 surfaces that anchor conditions 3/4/5), [`reports/field-report-04-falsification-evaluation.md`](reports/field-report-04-falsification-evaluation.md) (cph#31 full 6-condition synthesis).

## Phase R5 — New capture / friend pre-pilot

- **Goal:** Only after existing data justifies new capture, test the acquisition pipeline against a small friend cohort to evaluate consent, capture rehearsal, OpenCap output reliability, and the rest of the protocol stack.
- **Current evidence:** Protocols are drafted ([`protocols/friend-pre-pilot.md`](protocols/friend-pre-pilot.md), [`protocols/capture-rehearsal.md`](protocols/capture-rehearsal.md), [`protocols/capture-setup.md`](protocols/capture-setup.md), [`protocols/session-notes-template.md`](protocols/session-notes-template.md), [`protocols/blind-observation-memo.md`](protocols/blind-observation-memo.md), [`protocols/walking-conditions.md`](protocols/walking-conditions.md)). Ethics protocols are in place in [`docs/ethics/`](docs/ethics/). No friend data has been collected. No capture rehearsal has been executed.
- **Gate:** R3 and R4 have closed with GO; consent and capture rehearsal protocols pass dry-run; OpenCap output reliability on a friend cohort matches Lab Validation reference; `field-report-02-friend-pre-pilot` documents what survived, what failed, what was visible, and what was not.
- **Status:** Unblocked at the gate level (R3 closes on both R-side at partial GO and bilateral at indeterminate-on-inferred surface; R4 survives subject to caveats); the capture-protocol-revision step (≥3 s trial length so future captures do not recur the partial-clip geometric bias that drove cph#30's indeterminate H3 verdict) is the remaining R5 prerequisite. R5 has not transitioned to ACTIVE; the protocol-revision cycle owns the next R5 movement.
- **Coherence risk:** Friend data is read as validation of the hypothesis rather than as a pipeline-robustness test (the realization the project owns as [`docs/realizations/04-friends-are-a-pre-pilot.md`](docs/realizations/04-friends-are-a-pre-pilot.md)). Adjacent risks: body-typing or labeling participants, eroding the "object is the step under a condition, not the person" boundary; the safety-boundary sentence in [`README.md`](README.md) ("Under this condition, this recording shows this movement pattern") drifting toward typology language in protocol or report drafts.
- **Next action:** Wait for R3 and R4 to close GO. While waiting, run no friend captures; do not soft-start the cohort.
- **Owning files:** [`protocols/friend-pre-pilot.md`](protocols/friend-pre-pilot.md), [`protocols/capture-rehearsal.md`](protocols/capture-rehearsal.md), [`protocols/capture-setup.md`](protocols/capture-setup.md), [`protocols/walking-conditions.md`](protocols/walking-conditions.md), [`protocols/session-notes-template.md`](protocols/session-notes-template.md), [`protocols/blind-observation-memo.md`](protocols/blind-observation-memo.md), [`docs/ethics/`](docs/ethics/), [`reports/field-report-02-friend-pre-pilot.md`](reports/field-report-02-friend-pre-pilot.md) (stub; awaiting data).

## Phase R6 — AI classification

- **Goal:** Apply unsupervised or self-supervised structure-finding to per-cycle feature tables only after stable feature surfaces exist and survive confound checks.
- **Current evidence:** No clustering has been run. [`analysis/clustering-plan.md`](analysis/clustering-plan.md) exists as a plan-only document.
- **Gate:** A stable per-cycle feature table from R3 / R4 is available; unsupervised structure (clusters, embeddings, manifold organization) survives confound checks against trial crop, marker artifact, camera setup, subject morphology, and condition labels alone; clustering output is reported as candidate structure, not as confirmation of the seven gait families or the Coherence Path Hypothesis.
- **Status:** Not started.
- **Coherence risk:** The largest coherence risk in the project. Clustering output is the most likely surface to be read as hypothesis confirmation, since AI labels are easy to mistake for measurement. *Coherence laundering* — treating a clean clustering output (or a self-consistent repo) as substantive evidence for the hypothesis — is the failure mode to avoid. Adjacent risks: the seven gait families being reintroduced as proven categories by automated labels; the project's identity drifting from "test the hypothesis" toward "build a typology"; the safety boundary in [`README.md`](README.md) eroded by labels that read as diagnoses.
- **Next action:** Hold. Do not run clustering against existing or future data until R4 closes GO.
- **Owning files:** [`analysis/clustering-plan.md`](analysis/clustering-plan.md), future analysis documents.
